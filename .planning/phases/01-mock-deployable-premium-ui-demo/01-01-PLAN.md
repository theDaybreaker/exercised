---
phase: 01-mock-deployable-premium-ui-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - .gitignore
  - .env.example
  - .env.local
  - .prettierrc.json
  - vitest.config.ts
  - components.json
  - vercel.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/api/extract/route.ts
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/ui/card.tsx
  - components/ui/skeleton.tsx
  - components/ui/sonner.tsx
  - components/ui/tooltip.tsx
  - components/layout/AmbientBackground.tsx
  - components/layout/Footer.tsx
  - lib/utils.ts
  - lib/schema/workout.ts
  - lib/extraction/service.ts
  - lib/extraction/mock.ts
  - lib/extraction/real.ts
  - lib/sse/stream.ts
  - lib/youtube/url.ts
  - tests/fixtures/dumbbell-leg-day.json
  - tests/schema.test.ts
  - tests/youtube-url.test.ts
autonomous: false
requirements:
  - INPT-03
  - SCHM-01
  - SCHM-02
  - SCHM-03
  - SCHM-04
  - SCHM-05
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-06
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - OPS-01
  - OPS-03
user_setup:
  - service: vercel
    why: "Deploy the mock-mode demo to a public URL (OPS-01). User must own the Vercel project and connect GitHub for auto-deploy from main. Plan 01-02 ships the actual first deploy; Plan 01-01 prepares the project and env vars."
    env_vars:
      - name: EXTRACT_MODE
        source: "Vercel project → Settings → Environment Variables → add `EXTRACT_MODE` = `mock` for Preview + Production"
    dashboard_config:
      - task: "Connect GitHub repo to Vercel project so pushes to main auto-deploy"
        location: "Vercel Dashboard → Add New → Project → Import from GitHub"
      - task: "Set Vercel Spend Management cap at $20 (defense-in-depth even for mock-only)"
        location: "Vercel Dashboard → Team → Settings → Billing → Spend Management"

must_haves:
  truths:
    - "Fresh Next.js 16 + Tailwind 4 + shadcn project boots locally on `pnpm dev` with no console errors and no hydration warnings."
    - "`lib/schema/workout.ts` exports `WorkoutSchema` with `schema_version: z.literal(\"1\")`, a discriminated-union `RoutineItemSchema`, all SCHM-03 forward-looking fields (`startTimestamp`, `sourceQuote`, `equipment`), and `ExtractRequestSchema` + `ExtractEventSchema` discriminated unions for PIPE-01 wire shape."
    - "`tests/fixtures/dumbbell-leg-day.json` exists, contains `\"schema_version\": \"1\"`, includes at least one superset entry, and passes `WorkoutSchema.parse(...)` in `tests/schema.test.ts`."
    - "`lib/extraction/service.ts` exports the `ExtractionService` interface and `getExtractionService()` factory reading `EXTRACT_MODE`; `lib/extraction/real.ts` throws a clear 'not implemented in Phase 1' error so the failure mode is honest (T-01-07)."
    - "`lib/extraction/mock.ts` loads the dumbbell fixture at module init via `WorkoutSchema.parse(...)` (T-01-03), emits 4 stage events + 1 result event over ~4.3s (1100/1100/1100/1000ms dwells per D-07)."
    - "`POST /api/extract` responds with `Content-Type: text/event-stream` and the chunked body containing 5 events; route handler validates body with `ExtractRequestSchema.safeParse` and returns 400 with `issues` on schema failure (T-01-01) — verified by curl against `pnpm dev`."
    - "`app/globals.css` contains `@theme inline` (Tailwind 4 bridge, Pitfall 8 mitigation), `.glass-card` recipe with `backdrop-filter: blur(16px) saturate(140%)` + `@supports not (backdrop-filter)` fallback, and the master `@media (prefers-reduced-motion: reduce)` block."
    - "Geist Sans + Geist Mono are loaded via `geist/font/{sans,mono}` in `app/layout.tsx` with zero CLS (DSGN-04)."
    - "`<AmbientBackground />` renders 3 drifting orbs (green/violet/coral) behind glass via the `.orb-1/2/3` classes wired to the `@keyframes drift-orb-N` in globals.css (DSGN-01/02)."
    - "`<Footer />` renders the AI-disclaimer line per UI-SPEC §7.1 (deferred OUTV-06 surface — Plan 01-02 wires it into `app/page.tsx`)."
    - "Vercel project is created, GitHub-connected, and `EXTRACT_MODE=mock` is set in Preview + Production env vars (user_setup checkpoint)."
  artifacts:
    - path: "lib/schema/workout.ts"
      provides: "Zod WorkoutSchema with schema_version='1', discriminatedUnion routine, all forward-looking fields (startTimestamp/sourceQuote/equipment/extraction_confidence); ExtractRequestSchema; ExtractEventSchema; inferred TypeScript types"
      exports: ["WorkoutSchema", "RoutineItemSchema", "ExtractRequestSchema", "ExtractEventSchema", "Workout", "RoutineItem", "ExtractEvent", "ExtractRequest"]
      contains: "z.literal(\"1\")"
    - path: "lib/extraction/service.ts"
      provides: "ExtractionService interface and getExtractionService() factory reading EXTRACT_MODE"
      exports: ["ExtractionService", "getExtractionService"]
    - path: "lib/extraction/mock.ts"
      provides: "MockExtractionService that loads dumbbell-leg-day.json fixture at module init via WorkoutSchema.parse() and emits SSE stages with 1.0–1.2s dwell"
      exports: ["MockExtractionService"]
    - path: "lib/extraction/real.ts"
      provides: "Stub that throws so Phase 2 has a clear extension point and accidental EXTRACT_MODE=real fails loudly"
      exports: ["RealExtractionService"]
    - path: "lib/sse/stream.ts"
      provides: "toSSEStream(AsyncIterable) helper that produces a ReadableStream emitting `data: {json}\\n\\n` frames"
      exports: ["toSSEStream"]
    - path: "app/api/extract/route.ts"
      provides: "POST handler — Zod-validates body, gets service via factory, returns ReadableStream with SSE headers and X-Accel-Buffering: no"
      exports: ["POST", "runtime", "dynamic", "maxDuration"]
      contains: "maxDuration = 300"
    - path: "tests/fixtures/dumbbell-leg-day.json"
      provides: "Brief baseline fixture — 5–7 exercises including one superset, difficulty=intermediate, all schema fields populated (incl. forward-looking startTimestamp/sourceQuote/equipment for SCHM-03)"
      contains: "\"schema_version\": \"1\""
    - path: "app/globals.css"
      provides: "Tailwind 4 @theme block with Phase 1 design tokens, shadcn CSS-var bridge, .glass-card component class with backdrop-filter + saturate(140%), @supports fallback, @media (prefers-reduced-motion: reduce) master block"
      contains: "@theme inline"
    - path: "components/layout/AmbientBackground.tsx"
      provides: "RSC fixed-inset div with 3 child orbs (.orb-1/2/3) wired to globals.css drift keyframes — green/violet/coral, 60vw/70vw/40vw sizes, 80px/100px/70px blurs"
      exports: ["AmbientBackground"]
    - path: "components/layout/Footer.tsx"
      provides: "RSC AI-disclaimer + project credit line per UI-SPEC §7.1 (OUTV-06 surface, wired into page.tsx in Plan 01-02)"
      exports: ["Footer"]
    - path: "vercel.json"
      provides: "Minimal config — just $schema reference; Fluid Compute is default-on for new projects since 2025-04"
    - path: "tests/schema.test.ts"
      provides: "Vitest test that imports and WorkoutSchema.parse()es dumbbell-leg-day.json — fixture drift fails the build"
  key_links:
    - from: "app/api/extract/route.ts"
      to: "lib/extraction/service.ts"
      via: "getExtractionService() factory called inside POST handler"
      pattern: "getExtractionService\\("
    - from: "lib/extraction/mock.ts"
      to: "tests/fixtures/dumbbell-leg-day.json"
      via: "JSON import + WorkoutSchema.parse at module top-level"
      pattern: "WorkoutSchema\\.parse"
    - from: "app/layout.tsx"
      to: "geist/font/sans + geist/font/mono"
      via: "ESM imports + html className + sonner Toaster mount"
      pattern: "geist\\/font\\/(sans|mono)"
    - from: "app/page.tsx"
      to: "components/layout/AmbientBackground.tsx + components/layout/Footer.tsx"
      via: "RSC imports rendered at root of <main>; Plan 01-02 inserts <ExtractFlow /> between them"
      pattern: "AmbientBackground"
---

<objective>
Ship the Phase 1 foundation: scaffold a Next.js 16 + Tailwind 4 + shadcn project, lock the Zod `WorkoutSchema` (single source of truth for fixtures, mock SSE, and future Phase 2 `generateObject` calls), build the `MockExtractionService` + `getExtractionService` factory, ship the `POST /api/extract` SSE route handler, author the baseline `dumbbell-leg-day.json` fixture, and stand up the chrome layout (ambient orb background + footer + Tailwind 4 `@theme` block + Geist fonts). Vercel project is prepared (GitHub-connected, `EXTRACT_MODE=mock` set) so Plan 01-02 can deploy without dashboard work.

This plan establishes every load-bearing convention. Plan 01-02 layers the interactive UI on top (UrlInput, ExtractFlow, LoadingStages, WorkoutView, ActionBar, share encode/decode + clipboard, local verify + first Vercel deploy).

Purpose: The B1 checker blocker split the original 5-task Plan 01-01 into two plans by execution boundary — foundation (this plan, no interactive UI) and skeleton UI (Plan 01-02). The architectural decisions in 01-SKELETON.md are unchanged; the deliverables are just sliced at the natural seam between "bootable scaffold" and "user-visible interaction".

Output: A repo that boots cleanly with `pnpm dev`, serves an ambient-gradient landing page with a footer (no interactive UI yet — that ships in 01-02), exposes a working `POST /api/extract` SSE endpoint, and has a Vercel project ready to receive the first push from Plan 01-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-SKELETON.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- The Zod schema below is THE wire contract for Phase 1 and Phase 2. Implement EXACTLY this shape — fixture JSON, mock SSE output, and frontend types all derive from it. Verbatim copy from 01-RESEARCH.md §"Code Examples / Zod Schema". -->

From lib/schema/workout.ts (to be created in Task 2):
```ts
import { z } from "zod";

const ExerciseCoreSchema = z.object({
  exercise_name: z.string().min(1),
  sets: z.number().int().positive(),
  reps: z.string().min(1),                      // "10" | "8-12" | "AMRAP"
  rest_seconds: z.number().int().nonnegative(),
  form_cues: z.array(z.string()).default([]),
  startTimestamp: z.number().int().nonnegative().nullable(),  // SCHM-03 forward-looking
  sourceQuote: z.string().nullable(),                          // SCHM-03 forward-looking
  equipment: z.array(z.string()).default([]),                  // SCHM-03 forward-looking
});

const StandardSetSchema = ExerciseCoreSchema.extend({
  type: z.literal("standard_set"),
});

const SupersetSchema = z.object({
  type: z.literal("superset"),
  exercises: z.array(ExerciseCoreSchema).min(2),
  rest_seconds: z.number().int().nonnegative(),
});

export const RoutineItemSchema = z.discriminatedUnion("type", [
  StandardSetSchema, SupersetSchema,
]);

export const WorkoutSchema = z.object({
  schema_version: z.literal("1"),
  workout_title: z.string().min(1),
  creator_username: z.string().min(1),
  target_muscles: z.array(z.string()).default([]),
  estimated_duration_mins: z.number().int().positive(),
  difficulty: z.enum(["beginner", "intermediate", "advanced"]),
  extraction_confidence: z.enum(["high", "medium", "low"]),
  routine: z.array(RoutineItemSchema).min(1),
});

export const ExtractRequestSchema = z.object({ url: z.string().url() });

export const ExtractEventSchema = z.discriminatedUnion("type", [
  z.object({ type: z.literal("stage"), stage: z.enum(["fetching","transcribing","analyzing","generating"]) }),
  z.object({ type: z.literal("result"), workout: WorkoutSchema }),
  z.object({ type: z.literal("error"), code: z.enum(["NETWORK","NO_WORKOUT","RATE_LIMITED","UNKNOWN"]), message: z.string() }),
]);

export type Workout = z.infer<typeof WorkoutSchema>;
export type RoutineItem = z.infer<typeof RoutineItemSchema>;
export type ExtractEvent = z.infer<typeof ExtractEventSchema>;
export type ExtractRequest = z.infer<typeof ExtractRequestSchema>;
```

From lib/extraction/service.ts (to be created in Task 2):
```ts
export interface ExtractionService {
  extract(url: string): AsyncIterable<ExtractEvent>;
}
export async function getExtractionService(): Promise<ExtractionService>;
```

Design tokens (verbatim from UI-SPEC.md §4 + §5):
- `--color-bg-base`: `#08090C` — never pure `#000`
- `--color-surface-glass`: `rgba(255,255,255,0.11)` — 11% white tint on glass cards (per D-03)
- `--color-border-glass`: `rgba(255,255,255,0.10)`
- `--color-accent`: `#7CFF6B` — WCAG-tuned neon green (NOT the brief's #39FF14) (per D-01)
- `--color-accent-glow`: `rgba(124,255,107,0.35)`
- `--color-accent-muted`: `rgba(124,255,107,0.12)`
- `--color-text-primary`: `#F4F6F8` — off-white, NOT pure `#FFF`
- `--color-text-muted`: `#A8B0BC`
- `--color-destructive`: `#FF6B6B` (used in this plan only on the AI-disclaimer mark, full error UI in Plan 01-03)

Per-stage mock dwell (verbatim from CONTEXT.md D-07 + RESEARCH §"Code Examples / Mock Extraction Service"):
- fetching → sleep 1100ms → transcribing → sleep 1100ms → analyzing → sleep 1100ms → generating → sleep 1000ms → result
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api/extract` POST body | Untrusted JSON body crosses here; must be Zod-validated before any service call (ASVS V5) |
| `tests/fixtures/dumbbell-leg-day.json` → module load | Build-time-trusted asset; tampering caught by `WorkoutSchema.parse()` at module init crashing the function with a clear ZodError (ASVS V14) |
| `EXTRACT_MODE` env var → service factory | Trusted Vercel env var; misconfiguration (`real` in Phase 1) is caught by the explicit `RealExtractionService` throw in `lib/extraction/real.ts` (ASVS V14) |
| Vercel Function `/api/extract` → public internet | Unauthenticated public endpoint; Phase 1 logs requests only — rate limiting and global daily cap ship in Phase 2 per ROADMAP Phase 2 scope (RESEARCH §"Security Domain / Phase 1 security gate") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `/api/extract` POST body — malformed/oversized JSON | mitigate | `ExtractRequestSchema.safeParse(body)` returns 400 with structured Zod issues; `await req.json()` wrapped in try/catch returning 400 'Malformed JSON body' on parse failure (per RESEARCH §"Code Examples / SSE Route Handler"). Implement in Task 3. |
| T-01-03 | Tampering | `tests/fixtures/dumbbell-leg-day.json` schema drift | mitigate | `WorkoutSchema.parse(dumbbellLegDay)` called at module init in `lib/extraction/mock.ts` — schema drift crashes function with named ZodError before any traffic served. Vitest test `tests/schema.test.ts` runs `WorkoutSchema.parse` on the fixture in CI so drift fails the build, not production (RESEARCH §"Pitfall 3"). Implement in Task 2. |
| T-01-04 | Denial of Service | Unauthenticated `/api/extract` endpoint | accept (Phase 1) | Phase 1 is mock-only — every request runs synthetic setTimeout, no AI cost. Vercel Spend Management cap configured at $20 in user_setup as defense-in-depth. Rate limit + daily spend cap + Upstash cache ship in Phase 2 per ROADMAP (COST-01..04) in the same PR as the first real OpenAI key. Document this disposition in `lib/extraction/mock.ts` JSDoc. |
| T-01-05 | Tampering | Supply chain — npm install of `next`, `motion`, `zod`, `lz-string`, `geist`, `sonner`, `lucide-react`, `clsx`, `tailwind-merge`, `next-themes`, `@radix-ui/react-tooltip` | mitigate | All 11 packages pre-verified against npm registry on 2026-05-17 with source repos confirmed (see RESEARCH §"Package Legitimacy Audit"). `pnpm install --frozen-lockfile` after first install locks transitive versions in `pnpm-lock.yaml`. None have postinstall scripts (verified). Implement in Task 1. |
| T-01-SC | Tampering | npm package installation | mitigate | All Phase 1 packages have `[VERIFIED]` disposition in RESEARCH §"Package Legitimacy Audit" — no `[ASSUMED]`/`[SUS]`/`[SLOP]` items remain (the slopcheck false-positives were re-verified manually). No blocking-human checkpoint required. Lockfile pinned via `pnpm install --frozen-lockfile` after initial resolve in Task 1. |
| T-01-06 | Information Disclosure | Server logs leaking ZodError internals to client | mitigate | Route handler returns `{ error: "Invalid request", issues: parsed.error.issues }` for body validation failures — `issues` is the public Zod error shape (path + code + message), no stack traces. For internal errors (fixture parse, fetch failures), error events use `code: "UNKNOWN"` with a generic `message` and the full error is logged server-side via `console.error` only. Implement in Task 3. |
| T-01-07 | Information Disclosure | `EXTRACT_MODE` accidentally set to `real` in Phase 1 | mitigate | `lib/extraction/real.ts` stub explicitly throws `new Error('Real extraction not implemented — set EXTRACT_MODE=mock or implement Phase 2')` so the failure mode is visible in logs and the demo returns a clear `error: UNKNOWN` event rather than a generic 500 (RESEARCH §"Pitfall 9"). Implement in Task 2. |
| T-01-08 | Tampering | Clickjacking on deployed demo | accept (Phase 1) | Vercel default headers include `X-Frame-Options: SAMEORIGIN` for framework pages — verified via `curl -I` on first deploy in Plan 01-02. No custom CSP shipped in Phase 1 (no user-generated content, no embeds); custom CSP ships when Phase 2 introduces real user inputs (RESEARCH §"Security Domain"). |
</threat_model>

<tasks>

<task type="auto" gate="blocking-human">
  <name>Task 1: Bootstrap Next.js 16 + Tailwind 4 + shadcn + Vitest scaffold; tooling configs; ambient background + footer chrome; Tailwind 4 @theme + glass recipe in globals.css</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, .gitignore, .env.example, .env.local, .prettierrc.json, vitest.config.ts, components.json, vercel.json, app/layout.tsx, app/page.tsx, app/globals.css, components/ui/button.tsx, components/ui/input.tsx, components/ui/card.tsx, components/ui/skeleton.tsx, components/ui/sonner.tsx, components/ui/tooltip.tsx, lib/utils.ts, components/layout/AmbientBackground.tsx, components/layout/Footer.tsx</files>
  <read_first>
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md (D-01 through D-19 — every locked decision)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Standard Stack" + §"Installation" + §"Code Examples / Tailwind 4 @theme" + §"Code Examples / Layout with Geist Font" + §"Common Pitfalls / Pitfall 8" + §"Package Legitimacy Audit"
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §1 Design System + §2 Spacing + §3 Typography + §4 Color + §5 Glassmorphism Recipe
    - .planning/research/ARCHITECTURE.md (full file — RSC + Client island, Pattern 1)
    - .planning/research/STACK.md §"Recommended Stack / Core Technologies"
    - CLAUDE.md (project tech stack requirements)
  </read_first>
  <action>
Run pnpm-based bootstrap of a fresh Next.js 16 project under the repo root (no src/ subdir — `app/`, `components/`, `lib/`, `tests/` at top level per RESEARCH §"Recommended Project Structure"). Execute `pnpm create next-app@latest .` with flags `--typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"`. Then `pnpm dlx shadcn@latest init` answering: TypeScript yes, default style, base color `slate`, CSS variables yes, react-server-components yes, components dir `components/ui`, write `lib/utils.ts` for the `cn()` helper. Install Phase 1 runtime deps: `pnpm add zod motion geist lz-string lucide-react sonner @radix-ui/react-tooltip next-themes` (clsx + tailwind-merge come transitively via shadcn init). Install shadcn primitives one at a time: `pnpm dlx shadcn@latest add button input card skeleton sonner tooltip`. Install dev deps: `pnpm add -D vitest @vitest/ui prettier prettier-plugin-tailwindcss @types/lz-string`. After install, run `pnpm install --frozen-lockfile` to pin `pnpm-lock.yaml` per T-01-05 mitigation.

Pin exact versions in `package.json` to the RESEARCH §"Standard Stack" verified versions: `next@16.2.6`, `tailwindcss@4.3.0`, `zod@4.4.3`, `motion@12.38.0`, `geist@1.7.0`, `lz-string@1.5.0`, `lucide-react@1.16.0`, `sonner@2.0.7`, `clsx@2.1.1`, `tailwind-merge@3.6.0`, `next-themes@0.4.6`, `@radix-ui/react-tooltip@1.2.8`. `react@19.x` and `react-dom@19.x` are auto-resolved by Next 16.

Author `.prettierrc.json` with `{ "plugins": ["prettier-plugin-tailwindcss"] }` per CLAUDE.md `## Technology Stack` requirement (canonical Tailwind class ordering is non-negotiable). Author `vitest.config.ts` with the `tsconfig` paths plugin and node test environment. Author `tsconfig.json` paths `"@/*": ["./*"]` for the `@/lib/*` etc. import-alias.

Author `.env.example` with `EXTRACT_MODE=mock` (with comment explaining `mock | real`, default mock; per D-19 / PIPE-06). Author `.env.local` with `EXTRACT_MODE=mock`. Add `.env.local` to `.gitignore` (already there from create-next-app; confirm). Author `vercel.json` with literal contents `{ "$schema": "https://openapi.vercel.sh/vercel.json" }` per RESEARCH §"Assumption A10".

Author `app/layout.tsx` VERBATIM from RESEARCH §"Code Examples / Layout with Geist Font" — `import { GeistSans } from "geist/font/sans"; import { GeistMono } from "geist/font/mono";` plus `<html lang="en" className={`dark ${GeistSans.variable} ${GeistMono.variable}`} suppressHydrationWarning>` (D-02 single-family typography decision). Body uses `bg-[var(--color-bg-base)] text-[var(--color-text-primary)] font-sans antialiased`. Mount `<Toaster richColors theme="dark" position="bottom-center" />` from `sonner` (Plan 01-02 uses toasts for clipboard / share success). Metadata: title `"Exercised — Turn workout videos into structured routines"`, description per UI-SPEC §7.1 hero subhead.

Author `app/globals.css` VERBATIM from RESEARCH §"Code Examples / Tailwind 4 @theme" — `@import "tailwindcss";` + `:root { --background, --foreground, --primary (RGB tuples for #08090C, #F4F6F8, #7CFF6B), --accent, --ring, --border, --input }` + `@theme inline { --color-* bridge mapping all shadcn tokens + all Phase 1 custom tokens (--color-bg-base #08090C, --color-surface-glass rgb(255 255 255 / 0.11), --color-border-glass rgb(255 255 255 / 0.10), --color-text-primary #F4F6F8, --color-text-muted #A8B0BC, --color-accent-glow rgb(124 255 107 / 0.35), --color-accent-muted rgb(124 255 107 / 0.12), --color-destructive #FF6B6B, --color-destructive-muted rgb(255 107 107 / 0.12), --font-sans var(--font-geist-sans), --font-mono var(--font-geist-mono), --radius-glass 16px) }` (D-03 11% glass tint applied here). Then `@layer components { .glass-card { … verbatim §5.2 recipe with backdrop-filter: blur(16px) saturate(140%), -webkit-backdrop-filter, border, border-radius var(--radius-glass), box-shadow with depth + top-edge highlight … } }` + `@supports not (backdrop-filter: blur(1px)) { .glass-card { background-color: rgba(20,22,28,0.85); backdrop-filter: none; } }` + `@media (prefers-reduced-motion: reduce) { .orb-1, .orb-2, .orb-3 { animation: none !important; } .skeleton-shimmer { animation: none !important; } .pulse-dot { animation: none !important; } .card-hover-lift { transition: none !important; transform: none !important; } }` (D-05 reduced-motion master block). Add the three orb `@keyframes drift-orb-{1,2,3}` per UI-SPEC §5.1 (28s/36s/44s alternate ease-in-out, translate-only no rotate/scale).

Author `components/layout/AmbientBackground.tsx` as RSC — single fixed `<div>` with `inset-0 -z-10 pointer-events-none overflow-hidden bg-[var(--color-bg-base)]` containing 3 child orbs (`.orb-1`, `.orb-2`, `.orb-3`) per UI-SPEC §5.1 (green/violet/coral, sizes 60vw/70vw/40vw, blurs 80px/100px/70px, opacities baked into the gradient stops; D-03 ambient gradient with 2–3 vibrant color orbs drifting behind cards). No "use client" needed (D-04 single-screen centered composition — orbs are static markup; animation lives in CSS).

Author `components/layout/Footer.tsx` as RSC — renders the AI-disclaimer line from UI-SPEC §7.1 ("AI-extracted from public video content. Verify form, reps, and rest in the source video before training.") plus credit ("Exercised — a demo. v1.") in `--color-text-muted` Label-size type. OUTV-06 surface (will be wired into `app/page.tsx` in Plan 01-02).

Author `app/page.tsx` as a thin RSC: imports `<AmbientBackground />` and `<Footer />`, renders a placeholder `<main className="relative min-h-screen flex flex-col items-center justify-center px-6">` containing a temporary "Coming in next plan" hero placeholder (will be replaced by `<ExtractFlow />` in Plan 01-02). Layout per UI-SPEC §9.2.

PITFALL 8 verification: Add a temporary `<button className="bg-primary text-primary-foreground p-4">test</button>` to `app/page.tsx` and run `pnpm dev`. Inspect that `bg-primary` resolves to a green RGB value (not transparent). Then remove the test element before commit. If `bg-primary` is empty, the `@theme inline` bridge is wrong — fix it before proceeding.

User-setup checkpoint: this is a HUMAN CHECKPOINT GATE before continuing to Task 2 — pause and surface to the user the Vercel project creation steps (user_setup above) so they can wire GitHub → Vercel and add `EXTRACT_MODE=mock` to Preview + Production env vars BEFORE Plan 01-02 attempts to deploy. Resume signal: user replies "vercel ready" with project URL.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists exactly these runtime deps with these pinned versions: `next@16.2.6`, `react@19.x` (any patch), `react-dom@19.x`, `tailwindcss@4.3.0`, `zod@4.4.3`, `motion@12.38.0`, `geist@1.7.0`, `lz-string@1.5.0`, `lucide-react@1.16.0`, `sonner@2.0.7`, `clsx@2.1.1`, `tailwind-merge@3.6.0`, `next-themes@0.4.6`, `@radix-ui/react-tooltip@1.2.8`. Verify: `grep -E '"(next|zod|motion|geist|lz-string|sonner|tailwindcss|lucide-react|next-themes)"' package.json` returns 9+ lines.
    - `pnpm typecheck` (i.e. `tsc --noEmit`) exits 0.
    - `pnpm build` exits 0 and produces a `.next/` output.
    - `grep -c "@theme inline" app/globals.css` ≥ 1 (Tailwind 4 CSS-var bridge present, Pitfall 8 mitigation).
    - `grep -c "backdrop-filter" app/globals.css` ≥ 1 AND `grep -c "saturate(140%)" app/globals.css` ≥ 1 (glass recipe present per UI-SPEC §5.2).
    - `grep -c "@media (prefers-reduced-motion: reduce)" app/globals.css` ≥ 1 (master reduced-motion block present).
    - `grep -c "GeistSans\|GeistMono" app/layout.tsx` ≥ 2 (Geist Sans + Mono mounted, DSGN-04).
    - `grep -cE "orb-[123]" components/layout/AmbientBackground.tsx` ≥ 3 (three orb classes wired).
    - `grep -c "AI-extracted" components/layout/Footer.tsx` ≥ 1 (OUTV-06 disclaimer present).
    - User-setup checkpoint: Vercel project is created and `EXTRACT_MODE=mock` is set in Preview + Production env vars (user confirms "vercel ready" before Task 2 starts).
  </acceptance_criteria>
  <done>Fresh Next.js 16 + Tailwind 4 + shadcn project boots locally on `pnpm dev`; the placeholder landing page renders the ambient gradient background with three drifting orbs and the AI-disclaimer footer; no console errors, no hydration mismatch; Vercel project is ready to receive the first deploy in Plan 01-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zod schema (single source of truth) + baseline fixture + service factory + mock service + YouTube URL parser + Vitest tests</name>
  <files>lib/schema/workout.ts, lib/extraction/service.ts, lib/extraction/mock.ts, lib/extraction/real.ts, lib/youtube/url.ts, lib/sse/stream.ts, tests/fixtures/dumbbell-leg-day.json, tests/schema.test.ts, tests/youtube-url.test.ts</files>
  <behavior>
    - `WorkoutSchema.parse(dumbbellLegDay)` succeeds; parsed `schema_version === "1"`; `routine.length >= 1`; at least one routine entry has `type === "superset"`.
    - `parseYouTubeUrl("https://www.youtube.com/watch?v=dQw4w9WgXcQ")` returns `{ videoId: "dQw4w9WgXcQ", isValid: true, cleaned: "https://www.youtube.com/watch?v=dQw4w9WgXcQ" }`.
    - `parseYouTubeUrl("https://youtu.be/dQw4w9WgXcQ?si=abc&t=10")` returns `isValid: true` with `cleaned` having `si` and `t` stripped (INPT-03 tracking-param strip list).
    - `parseYouTubeUrl("https://youtube.com/shorts/aBcDeFgHiJk")` returns `isValid: true` with `videoId: "aBcDeFgHiJk"`.
    - `parseYouTubeUrl("https://vimeo.com/123")` returns `isValid: false`.
    - `MockExtractionService.extract(url)` consumed via `for await` yields exactly 5 events in order: 4 `{type:"stage", stage}` then 1 `{type:"result", workout}`. Per-stage dwell 1100/1100/1100/1000ms per D-07.
    - `RealExtractionService.extract(url)` immediately throws `Error` with message containing "not implemented" (T-01-07).
    - `getExtractionService()` returns `MockExtractionService` when `process.env.EXTRACT_MODE !== "real"` (PIPE-06 default-to-mock; per D-19).
  </behavior>
  <read_first>
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Code Examples / Zod Schema" + §"Code Examples / Mock Extraction Service" + §"Code Examples / YouTube URL Parser" + §"Code Examples / SSE Stream Helper" + §"Pattern 3 Service Factory" + §"Pitfall 9"
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §10 Fixture Visual Contract
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md §"Mock Fixture Strategy" (D-11, D-12, D-13, D-14 — note Plan 01-01 ships only the baseline; D-11 expansion ships in Plan 01-03)
    - .planning/research/ARCHITECTURE.md §"Pattern 3" (service factory) + §"Pattern 5" (schema-as-truth)
  </read_first>
  <action>
Author `lib/schema/workout.ts` VERBATIM from the `<interfaces>` Zod block above (D-14 fixture validation pattern depends on this schema; D-18 share-URL versioning depends on `schema_version: z.literal("1")`). Use Zod 4.4.x API; `schema_version` is `z.literal("1")` (NOT a number) for D-18 versioning across share URLs.

Author `lib/extraction/service.ts` with the `ExtractionService` interface and the `getExtractionService()` factory exactly per RESEARCH §"Architecture Patterns / Pattern 3" — dynamic import of `./real` only when `EXTRACT_MODE === "real"`, default fall-through to mock (PIPE-06, T-01-07 mitigation, D-19 mock-default policy).

Author `lib/extraction/real.ts` as a stub object `RealExtractionService` whose `extract` async generator immediately throws `new Error('Real extraction not implemented in Phase 1 — set EXTRACT_MODE=mock or implement RealExtractionService in Phase 2')`. This is the T-01-07 mitigation.

Author `lib/extraction/mock.ts` VERBATIM from RESEARCH §"Code Examples / Mock Extraction Service" but in Plan 01-01 import ONLY `dumbbellLegDay` from `tests/fixtures/dumbbell-leg-day.json`; FIXTURES array has one element. `hashStringMod` is still defined and used (always picks index 0 when length=1), so Plan 01-03's expansion is a one-line `FIXTURES` array additions. URL-keyword error branches (`fail`/`empty`/`rate-limit`) are NOT included in Plan 01-01 — they ship in Plan 01-03 per D-13. Per-stage dwell timing exactly per the `<interfaces>` block: 1100/1100/1100/1000ms (D-07 ~4–5s total dwell; D-10 stage transitions are timer-driven internally but emitted as SSE events). Module-load fixture validation per D-14: call `WorkoutSchema.parse(dumbbellLegDay)` at the top of the file so schema drift crashes init.

Author `lib/sse/stream.ts` VERBATIM from RESEARCH §"Code Examples / SSE Stream Helper" — `toSSEStream<T>(AsyncIterable<T>): ReadableStream<Uint8Array>` with `TextEncoder` + `controller.enqueue('data: ${json}\\n\\n')` + close-on-done + error event on iterator throw (Pitfall 6 mitigation prep).

Author `lib/youtube/url.ts` VERBATIM from RESEARCH §"Code Examples / YouTube URL Parser" — `parseYouTubeUrl(input): { videoId, isValid, cleaned }` with regex `^https?:\/\/(?:www\.)?(?:youtube\.com\/(?:watch\?(?:.*&)?v=|shorts\/)|youtu\.be\/)([A-Za-z0-9_-]{11})` and tracking-param strip list `["si","feature","t","list","index","pp","utm_source","utm_medium"]` (INPT-03 — paste-to-clean URL behavior).

Author `tests/fixtures/dumbbell-leg-day.json` — the brief baseline fixture. Required shape:
- `schema_version`: `"1"` (string literal)
- `workout_title`: `"Dumbbell-Only Leg Day · Hypertrophy Block"` (UI-SPEC §10 discretion sample)
- `creator_username`: `"kynanfit"` (UI-SPEC §10 synthetic handle; the synthetic-handle non-collision check happens once at SUMMARY documentation time per Plan 01-03 W5 fix — Plan 01-01 just picks a plausible handle and documents the choice in the SUMMARY without runtime validation)
- `target_muscles`: `["Quads","Glutes","Hamstrings","Calves"]`
- `estimated_duration_mins`: e.g. `45`
- `difficulty`: `"intermediate"` (accent chip per UI-SPEC §4.1)
- `extraction_confidence`: `"high"`
- `routine`: 5–7 entries total mixing `standard_set` and at least ONE `superset`. Each exercise carries: `exercise_name`, `sets` (int), `reps` (string — "10" / "8-12" / "AMRAP"), `rest_seconds` (int), `form_cues` (array of 2–4 strings), `startTimestamp` (int seconds or null), `sourceQuote` (string or null), `equipment` (array — e.g., `["dumbbells"]`). The superset has its own `rest_seconds` plus 2+ inner exercises.

Author `tests/schema.test.ts` — Vitest test that imports `dumbbell-leg-day.json` and asserts `WorkoutSchema.parse(...)` does not throw, and that the parsed result's `schema_version === "1"`, `routine.length >= 1`, and at least one routine entry has `type === "superset"`.

Author `tests/youtube-url.test.ts` — cases: `https://www.youtube.com/watch?v=dQw4w9WgXcQ` → valid videoId `dQw4w9WgXcQ`; `https://youtu.be/dQw4w9WgXcQ?si=abc` → valid + cleaned strips `si`; `https://youtube.com/shorts/aBcDeFgHiJk` → valid; `https://vimeo.com/123` → invalid; trailing whitespace → trimmed.

Run `pnpm test` and confirm both test files pass with ≥ 5 named assertions total.
  </action>
  <verify>
    <automated>pnpm test -- schema youtube-url && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 with `tests/schema.test.ts` and `tests/youtube-url.test.ts` PASS (at least 5 named assertions across the two files).
    - `pnpm typecheck` exits 0.
    - `grep -c "z.literal(\"1\")" lib/schema/workout.ts` ≥ 1 (schema_version is a literal, per D-18).
    - `grep -c "z.discriminatedUnion" lib/schema/workout.ts` ≥ 2 (RoutineItemSchema + ExtractEventSchema, per SCHM-05 + PIPE-01).
    - `grep -c "startTimestamp\|sourceQuote\|equipment" lib/schema/workout.ts` ≥ 3 (SCHM-03 forward-looking fields present).
    - `grep -c "WorkoutSchema.parse" lib/extraction/mock.ts` ≥ 1 (module-load fixture validation per T-01-03 / D-14).
    - `grep -c 'throw new Error("Real extraction not implemented' lib/extraction/real.ts` ≥ 1 (T-01-07 mitigation).
    - `grep -c "EXTRACT_MODE" lib/extraction/service.ts` ≥ 1 (PIPE-06 factory branch on env var).
    - `tests/fixtures/dumbbell-leg-day.json` parses against WorkoutSchema (covered by schema.test.ts).
    - `cat tests/fixtures/dumbbell-leg-day.json | grep -c '"type": "superset"'` ≥ 1 (fixture has at least one superset for SCHM-05 coverage).
    - `grep -c "1100\|1000" lib/extraction/mock.ts` ≥ 4 (per-stage dwell times present per D-07).
  </acceptance_criteria>
  <done>Zod schema is locked at `lib/schema/workout.ts`; the dumbbell-leg-day fixture parses successfully under both the standalone Vitest test and the module-load validation in `mock.ts`; the mock SSE generator emits 5 events with correct timing; the real-service stub fails loudly; `parseYouTubeUrl` handles all 5 test cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SSE Route Handler at /api/extract — Zod-validates body, returns text/event-stream with all 4 critical headers; curl smoke test</name>
  <files>app/api/extract/route.ts</files>
  <behavior>
    - `curl -sN -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}'` returns `Content-Type: text/event-stream` and a body containing 4 `data: {"type":"stage"…}` lines followed by 1 `data: {"type":"result",…}` line, in ~4–5s total (D-07 / PIPE-04 per-stage min dwell enforced by mock service, not by UI timers — D-10).
    - `curl -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{}'` returns HTTP 400 with `{ error: "Invalid request", issues: [...] }`.
    - `curl -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' --data-raw 'not-json'` returns HTTP 400 with `{ error: "Malformed JSON body" }`.
    - Response headers (verified via `curl -sI -X POST`): `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `Connection: keep-alive`, `X-Accel-Buffering: no` (all four required per Pitfall 1; missing any causes Vercel edge buffering).
  </behavior>
  <read_first>
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Code Examples / SSE Route Handler" + §"Common Pitfalls / Pitfall 1" + §"Common Pitfalls / Pitfall 6"
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §12 SSE Event UI Mapping
    - lib/schema/workout.ts (Task 2 — for `ExtractRequestSchema.safeParse`)
    - lib/extraction/service.ts (Task 2 — for the factory the route calls)
    - lib/sse/stream.ts (Task 2 — for `toSSEStream`)
  </read_first>
  <action>
Author `app/api/extract/route.ts` VERBATIM from RESEARCH §"Code Examples / SSE Route Handler":
- `export const runtime = "nodejs";` `export const dynamic = "force-dynamic";` `export const maxDuration = 300;` (OPS-03 + PIPE-01 + Pitfall 1 mitigation).
- POST handler:
  - Parse body with try/catch (400 'Malformed JSON body' on JSON parse failure per T-01-01).
  - `ExtractRequestSchema.safeParse(body)` returns 400 with `issues` field on schema failure (T-01-06 — no stack traces leaked to client).
  - Call `await getExtractionService()`, call `.extract(url)`, pass to `toSSEStream`.
  - Return `new Response(stream, { headers: { 'Content-Type': 'text/event-stream; charset=utf-8', 'Cache-Control': 'no-cache, no-transform', 'Connection': 'keep-alive', 'X-Accel-Buffering': 'no' } })` — ALL FOUR headers are required (Pitfall 1; missing any of them causes Vercel edge buffering).

Add inline source comment at top of file noting "Server emits SSE events; UI consumes events (never timers) — D-10 keeps the swap to real backend mechanical."

Run a manual curl smoke test against `pnpm dev`:
1. Start dev server in background: `pnpm dev > /tmp/exercised-dev.log 2>&1 &`
2. Wait 6s for boot, then `curl -sI -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}'` and confirm the four critical headers are present.
3. `curl -sN -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' --max-time 8 | tee /tmp/sse-out` and confirm 4 stage + 1 result events arrive.
4. `curl -sw "\\n%{http_code}" -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{}'` confirms 400.
5. `curl -sw "\\n%{http_code}" -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' --data-raw 'not-json'` confirms 400.
6. Kill dev server.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && (pnpm dev > /tmp/exercised-dev.log 2>&1 & DEV_PID=$!; sleep 6; HEADERS=$(curl -sI -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' --max-time 6); echo "$HEADERS" | grep -qi "content-type: text/event-stream" && echo "$HEADERS" | grep -qi "x-accel-buffering: no"; curl -sN -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' --max-time 8 > /tmp/sse-out; stage_count=$(grep -v '^#' /tmp/sse-out | grep -c '"type":"stage"' || echo 0); result_count=$(grep -v '^#' /tmp/sse-out | grep -c '"type":"result"' || echo 0); test "$stage_count" -eq 4 && test "$result_count" -eq 1; STATUS=$?; kill $DEV_PID 2>/dev/null; exit $STATUS)</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/extract/route.ts` exports `runtime = "nodejs"`, `dynamic = "force-dynamic"`, `maxDuration = 300`. Verify: `grep -cE '^export const (runtime|dynamic|maxDuration)' app/api/extract/route.ts` ≥ 3.
    - `app/api/extract/route.ts` sets all four critical SSE headers. Verify: `grep -cE 'text/event-stream|no-cache, no-transform|keep-alive|X-Accel-Buffering' app/api/extract/route.ts` ≥ 4.
    - `app/api/extract/route.ts` validates body via `ExtractRequestSchema.safeParse` and returns 400 with `issues` on failure. Verify: `grep -c "ExtractRequestSchema.safeParse" app/api/extract/route.ts` ≥ 1; `grep -c "status: 400" app/api/extract/route.ts` ≥ 2 (one for malformed JSON, one for Zod failure).
    - Live SSE smoke test (in `<verify>` block): a curl POST to the local dev server receives `Content-Type: text/event-stream` and a body containing exactly 4 `"type":"stage"` events and exactly 1 `"type":"result"` event, in ~4–5 seconds.
    - `pnpm typecheck` exits 0; `pnpm build` exits 0.
  </acceptance_criteria>
  <done>`POST /api/extract` is wired end-to-end: the route handler validates input, invokes the service factory, returns a streaming response with all four Pitfall 1 headers; curl smoke confirms 4 stage + 1 result events arrive in ~4–5s; malformed and invalid bodies produce 400s with structured error shapes.</done>
</task>

</tasks>

<verification>
  After Task 3 the following must all hold:
  - `pnpm install --frozen-lockfile && pnpm test && pnpm typecheck && pnpm build` exits 0 across all four commands.
  - `grep -c "z.literal(\"1\")" lib/schema/workout.ts` ≥ 1 (schema_version literal, D-18 prep).
  - `grep -c "@theme inline" app/globals.css` ≥ 1 (Tailwind 4 + shadcn var bridge).
  - `grep -c "backdrop-filter" app/globals.css` ≥ 1 AND `grep -c "saturate(140%)" app/globals.css` ≥ 1 (UI-SPEC §5.2 glass recipe).
  - `grep -c "X-Accel-Buffering" app/api/extract/route.ts` ≥ 1 (Pitfall 1 mitigation).
  - `grep -c "maxDuration = 300" app/api/extract/route.ts` ≥ 1 (OPS-03).
  - `grep -c "WorkoutSchema.parse" lib/extraction/mock.ts` ≥ 1 (D-14 module-load fixture validation).
  - Curl POST to local `/api/extract` returns 4 stage + 1 result SSE events in ~4–5s.
  - Vercel project is created and `EXTRACT_MODE=mock` set (user-confirmed checkpoint in Task 1).
</verification>

<success_criteria>
  Plan 01-01 is complete when:
  - The repo boots cleanly with `pnpm dev`; the landing page renders the dark ambient gradient with three drifting orbs and the AI-disclaimer footer.
  - The Zod schema is locked at `lib/schema/workout.ts` and validates the dumbbell baseline fixture.
  - The SSE route handler emits 4 stage + 1 result events via curl with the four critical headers.
  - The Vercel project is GitHub-connected with `EXTRACT_MODE=mock` set (Plan 01-02 just pushes and observes auto-deploy — no dashboard work mid-deploy).
  - All Vitest tests pass: schema fixture validation, YouTube URL parser (5+ named cases).
  - `pnpm typecheck` and `pnpm build` exit 0.

  This plan is the foundation for Plan 01-02 (skeleton UI: UrlInput + ExtractFlow + LoadingStages + WorkoutView + ExerciseCard + SupersetCard + ActionBar + share encode/decode + clipboard + local verify + first Vercel deploy).
</success_criteria>

<output>
Create `.planning/phases/01-mock-deployable-premium-ui-demo/01-01-SUMMARY.md` when done, capturing:
- Final pinned package versions (in case any auto-bumped during install).
- The final synthetic creator handle chosen for `dumbbell-leg-day.json` and rationale (handle non-collision is documented here as a planning-time concern, not runtime-validated per W5 fix — pick a plausible handle and note "synthetic, no runtime collision check").
- Confirmed-working SSE chunk count and timing observed in local curl.
- Any Pitfall 1/8/9 anomalies surfaced during dev and how they were fixed.
- A "Plan 01-02 hand-off" note: the interfaces locked in this plan (WorkoutSchema, ExtractEventSchema, ExtractionService, parseYouTubeUrl, toSSEStream); the dumbbell fixture available for UI rendering; the Vercel project ready for first deploy.
</output>
</content>
</invoke>