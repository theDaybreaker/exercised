---
phase: 01-mock-deployable-premium-ui-demo
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/page.tsx
  - components/extract/ExtractFlow.tsx
  - components/extract/UrlInput.tsx
  - components/extract/LoadingStages.tsx
  - components/extract/reducer.ts
  - components/workout/SkeletonCard.tsx
  - components/workout/WorkoutView.tsx
  - components/workout/WorkoutHeader.tsx
  - components/workout/ExerciseCard.tsx
  - components/workout/SupersetCard.tsx
  - components/workout/MusclePill.tsx
  - components/workout/DifficultyChip.tsx
  - components/workout/DurationChip.tsx
  - components/workout/ActionBar.tsx
  - lib/share/encode.ts
  - lib/share/decode.ts
  - lib/schema/workout.ts
  - lib/clipboard/markdown.ts
  - lib/clipboard/plaintext.ts
  - tests/share-url-roundtrip.test.ts
  - tests/clipboard.test.ts
  - tests/reducer.test.ts
  - README.md
  - .planning/STATE.md
autonomous: false
requirements:
  - INPT-01
  - INPT-02
  - PIPE-05
  - OUTV-01
  - OUTV-03
  - OUTV-04
  - OUTV-05
  - OUTV-06
  - SHRE-01
  - SHRE-02
  - SHRE-03
  - OPS-02

must_haves:
  truths:
    - "A user can paste any YouTube URL (e.g., https://www.youtube.com/watch?v=dQw4w9WgXcQ) into the landing-page input and press Enter or click Extract Workout (INPT-01)."
    - "After submit, the user sees four loading-stage labels animate in sequence ('Fetching video data…' → 'Transcribing audio…' → 'Analyzing form cues…' → 'Generating routine…') driven by real SSE events arriving from /api/extract over ~4–5 seconds total (per D-07 / D-09 / D-10)."
    - "While loading, four skeleton workout cards are visible beneath the active stage label (PIPE-05)."
    - "After the result event arrives, the loading view dissolves and the dumbbell-leg-day fixture renders in glass cards with a Motion-staggered cascade — header first, then exercise cards 65ms apart (per D-08 all-at-once + staggered cascade)."
    - "The rendered workout displays workout title, creator handle (@…), estimated duration chip, difficulty chip, target-muscle pills, exercise cards with sets/reps/rest in Geist Mono, the superset rendered with a bracketed visual grouping + shared rest indicator (OUTV-01/03/04), a 'Watch on YouTube' link (OUTV-05), and an AI-disclaimer footer (OUTV-06)."
    - "The user can click 'Copy as Markdown' and paste a readable markdown representation of the workout (SHRE-01)."
    - "The user can click 'Copy as Plain Text' and paste a Notes/WhatsApp-friendly plain-text representation (SHRE-02)."
    - "The user can click 'Share Workout' which generates a `?w=...` URL containing the lz-string-compressed workout via the SharePayloadSchema wrapper (W6 fix — wire format is stable from first deploy and survives Plan 01-04's strip-chain addition), copies it to clipboard, and shows a toast confirmation (SHRE-03)."
    - "Opening that share URL in another tab/browser jumps directly to the rendered workout view (no SSE stream, no cascade animation — instant render) with a visually identical layout (D-16 share-link hydration on mount + D-19 refresh-without-w resets to idle)."
    - "Pasting an invalid URL (e.g., https://vimeo.com/123) blocks submission — no `/api/extract` call is made; the input shows a simple inline error 'Please enter a valid YouTube URL' (INPT-02 minimal — Plan 01-03 ships the full inline error with `aria-invalid` polish + tracking-param paste handler)."
    - "Live Vercel deploy on `$PROD_URL` from `main` branch via GitHub integration; `curl -sI -X POST $PROD_URL/api/extract ...` confirms `Content-Type: text/event-stream` + `X-Accel-Buffering: no` in production (Pitfall 1 verified live); cross-browser share-link round-trip works (OPS-02 acceptance gate)."
  artifacts:
    - path: "components/extract/reducer.ts"
      provides: "Pure reducer + initialState for the FSM (idle | submitting | streaming | success | error) per D-15 + D-16; actions: submit | stage | success | error | hydrate | reset; D-19 reset behavior"
      exports: ["reducer", "initialState", "State", "Action", "Stage", "ErrorCode"]
    - path: "components/extract/ExtractFlow.tsx"
      provides: "Single Client island — owns useReducer, fetch().body.getReader() SSE loop, share-link hydration on mount via ?w param + decodeShareUrl, renders one of UrlInput / LoadingStages / WorkoutView / error fallback"
      exports: ["ExtractFlow"]
    - path: "components/extract/LoadingStages.tsx"
      provides: "4 stage labels with pulse-dot/checkmark indicators + 4 skeleton cards beneath; aria-live='polite' announcements (per D-09)"
      exports: ["LoadingStages"]
    - path: "components/workout/WorkoutView.tsx"
      provides: "Renders WorkoutHeader + ordered ExerciseCard/SupersetCard list with Motion stagger cascade; respects useReducedMotion()"
      exports: ["WorkoutView"]
    - path: "components/workout/ActionBar.tsx"
      provides: "Inline row of Watch on YouTube + Copy MD + Copy Plain + Share Workout buttons; clipboard + lz-string wired with sonner toast confirmations"
      exports: ["ActionBar"]
    - path: "lib/schema/workout.ts"
      provides: "EXTENDED in this plan to add SharePayloadSchema wrapper + StripFieldSchema (W6 — wire format defined upfront, stripped:[] no-op default; Plan 01-04 only adds strip-chain logic, no schema change)"
      exports: ["SharePayloadSchema", "StripFieldSchema", "StripField", "SharePayload"]
      contains: "SharePayloadSchema"
    - path: "lib/share/encode.ts"
      provides: "encodeShareUrl(workout) → { encoded, stripped: [] } using lz-string compressToEncodedURIComponent against SharePayloadSchema wrapper (W6 — no-op strip chain placeholder; Plan 01-04 fills the loop body)"
      exports: ["encodeShareUrl"]
    - path: "lib/share/decode.ts"
      provides: "decodeShareUrl(encoded) → { workout, stripped } via lz-string decompressFromEncodedURIComponent + SharePayloadSchema.safeParse with schema_version error mapping (D-18) + 50KB DoS sanity cap (T-01-02)"
      exports: ["decodeShareUrl"]
    - path: "lib/clipboard/markdown.ts"
      provides: "workoutToMarkdown(w: Workout): string — # title + by @handle + difficulty + duration + target muscles + ## exercise headers + Geist-style sets×reps@rest + form_cues bullets + supersets indented (SHRE-01)"
      exports: ["workoutToMarkdown"]
    - path: "lib/clipboard/plaintext.ts"
      provides: "workoutToPlainText(w: Workout): string — Notes/WhatsApp-friendly format, no markdown markers (SHRE-02)"
      exports: ["workoutToPlainText"]
    - path: "README.md"
      provides: "Project description + Local development + Deployment + Architecture sections (links to 01-SKELETON.md)"
    - path: ".planning/STATE.md"
      provides: "stopped_at + last_activity updated with Plan 01-02 ship to $PROD_URL"
  key_links:
    - from: "components/extract/ExtractFlow.tsx"
      to: "/api/extract"
      via: "fetch('/api/extract', { method: 'POST', body: JSON.stringify({ url }) }) + res.body.pipeThrough(new TextDecoderStream()).getReader()"
      pattern: "fetch\\(['\"]\\/api\\/extract['\"]"
    - from: "components/extract/ExtractFlow.tsx"
      to: "lib/share/decode.ts"
      via: "useEffect on mount reads window.location.search ?w param, calls decodeShareUrl, dispatches hydrate (D-16)"
      pattern: "decodeShareUrl\\("
    - from: "components/workout/ActionBar.tsx"
      to: "lib/share/encode.ts"
      via: "onClick handler calls encodeShareUrl, builds URL with window.location.origin, calls navigator.clipboard.writeText, fires sonner toast"
      pattern: "encodeShareUrl\\("
    - from: "lib/share/encode.ts + lib/share/decode.ts"
      to: "lib/schema/workout.ts SharePayloadSchema"
      via: "JSON wire shape { workout, stripped } encoded/decoded against the wrapper schema (W6 — stable wire format from first deploy)"
      pattern: "SharePayloadSchema"
---

<objective>
Layer the interactive skeleton UI onto the foundation shipped by Plan 01-01. Build the `<ExtractFlow />` Client island that owns the FSM reducer, the SSE consumer loop, the share-link hydration mount-effect, and the four rendered states (UrlInput / LoadingStages / WorkoutView / error fallback). Build the workout render tree (WorkoutHeader + ExerciseCard + SupersetCard + chips/pills + ActionBar). Ship clipboard exporters (markdown + plaintext) and the share-link encode/decode round-trip against the `SharePayloadSchema` wrapper (W6 fix — the wrapper is defined upfront so Plan 01-04's strip-chain addition is internal-only, no wire-format change). Local-verify the happy path end-to-end via a human checkpoint, then deploy to Vercel from `main` and confirm OPS-02 acceptance (cross-browser/cross-device share-link round-trip on the live URL).

Purpose: The B1 checker blocker split the original 5-task Plan 01-01 into two plans. Plan 01-01 ships the bootable foundation (scaffold + schema + service + SSE route + chrome). This Plan 01-02 ships the user-visible interaction surface and the first production deploy.

The W6 fix lives in this plan: `SharePayloadSchema` wrapper definition + wrapper-encoding is introduced HERE (not in Plan 01-04). Initial wrapper uses `stripped: []` as a no-op default. Plan 01-04 only adds the strip-chain logic that progressively populates `stripped[]` when compressed size exceeds 2KB — wire format is stable from first deploy, so no in-flight share URLs ever break.

Output: A deployed Vercel URL where pasting any YouTube URL streams the SSE loading cascade over skeleton cards, the dumbbell-leg-day fixture renders in glass cards with the Motion cascade, Copy as Markdown / Copy as Plain Text / Share Workout all work, and the share URL opens correctly in a second browser/device (OPS-02 acceptance gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-SKELETON.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-01-PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@app/page.tsx
@app/layout.tsx
@app/globals.css
@app/api/extract/route.ts
@lib/schema/workout.ts
@lib/extraction/mock.ts
@lib/extraction/service.ts
@lib/sse/stream.ts
@lib/youtube/url.ts
@tests/fixtures/dumbbell-leg-day.json
@components/layout/AmbientBackground.tsx
@components/layout/Footer.tsx

<interfaces>
<!-- Locked from Plan 01-01: WorkoutSchema, RoutineItemSchema, ExtractRequestSchema, ExtractEventSchema, ExtractionService, getExtractionService, parseYouTubeUrl, toSSEStream — all exported from their respective files in lib/. -->

<!-- This plan ADDS to lib/schema/workout.ts (W6 — share wrapper introduced upfront so wire format is stable across the Phase 1 release): -->
```ts
export const StripFieldSchema = z.enum(["sourceQuote", "form_cues", "equipment"]);
export const SharePayloadSchema = z.object({
  workout: WorkoutSchema,
  stripped: z.array(StripFieldSchema).default([]),
});
export type StripField = z.infer<typeof StripFieldSchema>;
export type SharePayload = z.infer<typeof SharePayloadSchema>;
```

<!-- W6 explicit: the wire format for ?w= payloads is `{ workout: <Workout>, stripped: <StripField[]> }`. Plan 01-02 encodes/decodes against THIS wrapper. The `stripped: []` field is a no-op default in Plan 01-02 — the strip-chain logic in Plan 01-04 populates it progressively. Because the wrapper is defined now, Plan 01-04 only changes encode/decode internals, not the wire shape. -->

Stage labels (verbatim from UI-SPEC §7.2; CONTEXT.md D-06):
- `fetching` → active: "Fetching video data…" | completed: "Fetching video data"
- `transcribing` → active: "Transcribing audio…" | completed: "Transcribing audio"
- `analyzing` → active: "Analyzing form cues…" | completed: "Analyzing form cues"
- `generating` → active: "Generating routine…" | completed: "Generating routine"

Reducer state machine (verbatim per RESEARCH §"Code Examples / State Machine Reducer", D-15):
- States: `idle | submitting | streaming | success | error`
- Actions: `submit | stage | success | error | hydrate | reset`
- `success` action sets `fromShareLink: false`; `hydrate` action sets `fromShareLink: true` (D-16)
- `reset` returns `initialState` (D-19 — refresh without ?w resets to idle)
- `stage` action computes `completedStages` from `STAGE_ORDER.slice(0, indexOf(action.stage))` so out-of-order stage events still produce a consistent UI state.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser clipboard paste → URL input | Untrusted string; client-side YouTube regex validation gates the `/api/extract` POST (ASVS V5). Full inline error UI + tracking-param paste handler ship in Plan 01-03. |
| `?w=<lz-string>` query param hydration | DESERIALIZATION SURFACE — payload is lz-string-decompressed, JSON.parsed, then `SharePayloadSchema.safeParse`-validated; only schema-valid output reaches React render (ASVS V5 + V7). 50,000-char sanity cap before parsing per RESEARCH §"DoS via large ?w= payload". |
| Live Vercel public endpoint at `$PROD_URL` | Inherits Plan 01-01's T-01-04 disposition: mock-only, no AI cost; Vercel Spend Management cap defense-in-depth. Phase 2 ships rate limit + cache + daily cap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Information Disclosure | Share-link `?w=` payload deserialization | mitigate | `lib/share/decode.ts` calls `LZString.decompressFromEncodedURIComponent` (returns null on tamper), then `JSON.parse` inside try/catch, then `SharePayloadSchema.safeParse` — only schema-valid output reaches `dispatch({ type: 'hydrate', workout, stripped })`. Decode failure dispatches `error` action with friendly message. No `dangerouslySetInnerHTML` anywhere; React text-node escaping covers reflected content. Sanity-cap decoded JSON length at 50,000 chars before parsing. Implement in Task 2. |
| T-02-02 | Information Disclosure | D-18 schema-version mismatch leaking internal details | mitigate | `decodeShareUrl` inspects the SharePayloadSchema safeParse error issues — if any issue path contains `schema_version` (either as `workout.schema_version` or root `schema_version` for legacy bare-workout payloads), throw the friendly D-18 message ("This share link was created with a newer version of Exercised — try pasting the original YouTube URL instead."). Other validation failures throw a generic "Share link payload failed validation" message. Implement in Task 2. |
| T-02-08 | Tampering | Clickjacking on deployed demo | accept (Phase 1) | Vercel default headers include `X-Frame-Options: SAMEORIGIN` for framework pages — verified via `curl -I` on first deploy in Task 5. No custom CSP shipped in Phase 1 (no user-generated content, no embeds); custom CSP ships when Phase 2 introduces real user inputs. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE consumer + ExtractFlow Client island + LoadingStages + reducer FSM + UrlInput + SkeletonCard</name>
  <files>app/api/extract/route.ts, components/extract/ExtractFlow.tsx, components/extract/UrlInput.tsx, components/extract/LoadingStages.tsx, components/extract/reducer.ts, components/workout/SkeletonCard.tsx, app/page.tsx, tests/reducer.test.ts, app/globals.css</files>
  <behavior>
    - Reducer test: `reducer({ kind: 'idle' }, { type: 'submit', url: 'https://youtube.com/watch?v=x' })` returns `{ kind: 'submitting', url: 'https://youtube.com/watch?v=x' }`.
    - Reducer test: `reducer({ kind: 'submitting', url: 'u' }, { type: 'stage', stage: 'transcribing' })` returns `{ kind: 'streaming', url: 'u', currentStage: 'transcribing', completedStages: ['fetching'] }`.
    - Reducer test: `reducer({ kind: 'streaming', ... }, { type: 'success', workout: <valid> })` returns `{ kind: 'success', workout: <valid>, fromShareLink: false }`.
    - Reducer test: `reducer({ kind: 'streaming', ... }, { type: 'hydrate', workout: <valid>, stripped: [] })` returns `{ kind: 'success', workout: <valid>, fromShareLink: true, stripped: [] }`.
    - Reducer test: `reducer(state, { type: 'reset' })` returns `initialState` (D-19).
    - End-to-end (manual + automated curl): paste a YouTube URL on `/`, observe the 4 stage labels animate sequentially (verbatim copy from UI-SPEC §7.2), see 4 skeleton glass cards beneath, state transitions to `success` within ~5s — no `setTimeout`-driven UI stage changes (D-10 — UI consumes events, never timers; PITFALLS §7).
  </behavior>
  <read_first>
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Code Examples / State Machine Reducer" + §"Code Examples / SSE Consumer" + §"Common Pitfalls / Pitfall 6"
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §6.1 Stage cascade + §6.2 Result reveal + §7.2 Loading copy + §12 SSE Event UI Mapping + §14 Accessibility Contract
    - lib/schema/workout.ts (from Plan 01-01 — for the ExtractEventSchema discriminated union the SSE consumer parses against)
    - lib/extraction/mock.ts (from Plan 01-01 — for the SSE event timing reference)
    - app/page.tsx (from Plan 01-01 — to replace the placeholder hero with <ExtractFlow />)
  </read_first>
  <action>
Author `components/extract/reducer.ts` VERBATIM from RESEARCH §"Code Examples / State Machine Reducer": 5 states (`idle | submitting | streaming | success | error`), 6 actions (`submit | stage | success | error | hydrate | reset`). The `success` action sets `fromShareLink: false`; the `hydrate` action sets `fromShareLink: true` AND `stripped: action.stripped` (this is the W6 path — the `hydrate` action carries the strip-flag info that the recipient view will need; Plan 01-02 just plumbs `[]` through, Plan 01-04 starts populating it). The `stage` action computes `completedStages` from `STAGE_ORDER.slice(0, indexOf(action.stage))` so out-of-order stage events still produce a consistent UI state. The `reset` action returns `initialState` per D-19 (refresh without ?w resets to idle — no localStorage caching, keeps the model honest).

Author `components/workout/SkeletonCard.tsx` as `"use client"` — a single glass-card with 3 internal `<div>` rows of widths 70%/40%/90% with a `skeleton-shimmer` class. Define the shimmer keyframes in `app/globals.css` (UI-SPEC §6.1 last paragraph): `@keyframes shimmer-sweep { from { transform: translateX(-100%); } to { transform: translateX(100%); } }` applied to a `linear-gradient` pseudo-element 1.6s infinite. The `prefers-reduced-motion` block in globals.css already nukes this animation (already done in Plan 01-01).

Author `components/extract/LoadingStages.tsx` as `"use client"` — accepts props `{ currentStage: Stage; completedStages: ReadonlyArray<Stage> }`. Renders title "Building your workout" (UI-SPEC §7.2) above a vertical list of 4 stage rows (D-09 single active stage label with subtle pulse on the indicator dot, previous-stage checkmarks rendered as small icons beside it, 3–4 skeleton workout cards visible below the active stage). Each row:
- if in `completedStages`: lucide `Check` icon in `--color-accent` at 60% opacity + completed label (no trailing ellipsis per UI-SPEC §7.2 table)
- if equal to `currentStage`: pulse-dot (8px circle in `--color-accent` with `animate-pulse` class — define `.pulse-dot` keyframes in globals.css: `@keyframes pulse-dot { 0%,100% { transform: scale(1); } 50% { transform: scale(1.25); } }` 1.4s infinite ease-in-out) + active label (with trailing `…`)
- otherwise upcoming: hollow dot (8px circle with 1px `--color-accent` border, transparent fill) + label in `--color-text-muted`
Wrap the active label in `<div role="status" aria-live="polite">` per UI-SPEC §14 (SR announces stage transitions). Use Motion `AnimatePresence` with `mode="popLayout"` to animate the dot→checkmark transition over 220ms cubic-bezier(0.22, 1, 0.36, 1) and the label-color fade over 180ms (UI-SPEC §6.1). Below the stage list render 4 `<SkeletonCard />` instances (PIPE-05).

Author `components/extract/UrlInput.tsx` as `"use client"` — accepts props `{ onSubmit: (url: string) => void; isLoading: boolean }`. Renders a glass-styled input frame (UI-SPEC §5.2 .glass-card recipe via className) containing the shadcn `<Input>` with placeholder "Paste a YouTube workout video URL" + aria-label "YouTube video URL" (UI-SPEC §7.1). Below the input, a large `<Button size="lg">` (height 56px = `h-14`, UI-SPEC §2) with label "Extract Workout" (INPT-01). Handles Enter key on the input and click on the button identically. Disables the button when isLoading is true. Validates the URL on submit via `parseYouTubeUrl().isValid` — if invalid, sets local error state and renders a simple "Please enter a valid YouTube URL" line in `--color-destructive` below the input (INPT-02 minimal — full inline error with `aria-invalid` / `aria-describedby` polish + tracking-param paste handler ship in Plan 01-03). On valid submit, calls `onSubmit(cleaned)` where `cleaned` is the URL with tracking params stripped (INPT-03 — `parseYouTubeUrl` from Plan 01-01 already strips). The `parseYouTubeUrl` import comes from `@/lib/youtube/url`.

Author `components/extract/ExtractFlow.tsx` as `"use client"` based on RESEARCH §"Code Examples / SSE Consumer": owns `useReducer(reducer, initialState)`; `useRef<AbortController>` for the active fetch; `useEffect` on mount checks `new URLSearchParams(window.location.search).get("w")` and if present calls `decodeShareUrl(param)` inside try/catch — success dispatches `{type:'hydrate', workout, stripped}`, failure dispatches `{type:'error', code:'UNKNOWN', message}` (D-16 share-link hydration + T-02-01 mitigation). NOTE: `decodeShareUrl` doesn't exist yet — Task 2 creates it. For Task 1, mock the import temporarily or order the work so Task 2 lands the share modules first. Easiest: do Task 2 first inside this plan, then this hydrate path is wired natively. The `handleSubmit(url)` function: abort prior fetch via `abortRef.current?.abort()`; create new AbortController; dispatch `submit`; `fetch('/api/extract', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({url}), signal: ac.signal })`; if `!res.ok || !res.body` dispatch error; otherwise `res.body.pipeThrough(new TextDecoderStream()).getReader()`; loop reads, buffers, splits on `\n\n`, for each `data:` line `JSON.parse` + `ExtractEventSchema.parse`, dispatch matching action. Renders one of four UI shapes based on `state.kind`:
- `idle` → `<UrlInput onSubmit={handleSubmit} isLoading={false} />`
- `submitting | streaming` → `<LoadingStages currentStage={state.kind === 'streaming' ? state.currentStage : 'fetching'} completedStages={state.kind === 'streaming' ? state.completedStages : []} />` (during the brief `submitting` window before the first SSE arrives, default to `fetching` upcoming — i.e. all 4 dots hollow)
- `success` → `<WorkoutView workout={state.workout} shouldAnimateIn={!state.fromShareLink} />` (Task 3 creates WorkoutView)
- `error` → in Plan 01-02, render a minimal fallback `<div role="alert">Sorry, something went wrong: {state.message}</div>` — full ErrorState UI ships in Plan 01-03.

Update `app/page.tsx` (RSC) to mount `<ExtractFlow />` inside the `<main>` previously authored in Plan 01-01 (replacing the placeholder hero). Also wire `<Footer />` into the main layout (OUTV-06 — was authored in Plan 01-01 but not yet inserted into the page). The hero composition (UI-SPEC §9.2) wraps `<ExtractFlow />` in a `max-w-2xl` container; the page already has `flex flex-col items-center justify-center` from Plan 01-01.

Add reducer Vitest tests `tests/reducer.test.ts` covering all 5 behavior cases listed above.
  </action>
  <verify>
    <automated>pnpm test -- reducer && pnpm typecheck && pnpm build && (pnpm dev > /tmp/exercised-dev.log 2>&1 & DEV_PID=$!; sleep 6; curl -sN -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' --max-time 8 > /tmp/sse-out; stage_count=$(grep -v '^#' /tmp/sse-out | grep -c '"type":"stage"' || echo 0); result_count=$(grep -v '^#' /tmp/sse-out | grep -c '"type":"result"' || echo 0); test "$stage_count" -eq 4 && test "$result_count" -eq 1; STATUS=$?; kill $DEV_PID 2>/dev/null; exit $STATUS)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/reducer.test.ts` has ≥ 5 named test cases (submit/stage/success/hydrate/reset) all passing.
    - Source assertion: `components/extract/ExtractFlow.tsx` uses `fetch().body.pipeThrough(new TextDecoderStream()).getReader()` pattern (verifies: `grep -c "TextDecoderStream" components/extract/ExtractFlow.tsx` ≥ 1) and parses each event via `ExtractEventSchema.parse` (verifies: `grep -c "ExtractEventSchema.parse" components/extract/ExtractFlow.tsx` ≥ 1).
    - Source assertion: `components/extract/ExtractFlow.tsx` mounts a `useEffect` that reads `window.location.search` for `?w=` (verifies: `grep -c '\.get(.w.)' components/extract/ExtractFlow.tsx` ≥ 1).
    - Source assertion: `components/extract/LoadingStages.tsx` renders the verbatim UI-SPEC §7.2 stage labels — `grep -c 'Fetching video data\|Transcribing audio\|Analyzing form cues\|Generating routine' components/extract/LoadingStages.tsx` ≥ 4.
    - Source assertion: `components/extract/LoadingStages.tsx` contains `role="status"` and `aria-live="polite"` (UI-SPEC §14 SR announcement). Verify: `grep -c 'role="status"' components/extract/LoadingStages.tsx` ≥ 1; `grep -c 'aria-live="polite"' components/extract/LoadingStages.tsx` ≥ 1.
    - NO setTimeout-driven UI stage cycling per D-10. Verify: `grep -nE 'setTimeout.*stage|setInterval.*stage' components/extract/*.tsx` returns no matches.
    - `grep -cE 'setTimeout|setInterval' components/extract/ExtractFlow.tsx components/extract/LoadingStages.tsx components/extract/reducer.ts` returns 0.
    - `app/page.tsx` imports and renders `<ExtractFlow />` AND `<Footer />`. Verify: `grep -c "ExtractFlow" app/page.tsx` ≥ 1; `grep -c "Footer" app/page.tsx` ≥ 1.
    - Live SSE smoke test (in `<verify>` block): a curl POST to the local dev server receives the expected 4 stage + 1 result events in ~4–5 seconds.
  </acceptance_criteria>
  <done>`pnpm dev` runs locally; pasting a YouTube URL on `/` produces the SSE-driven 4-stage cascade visible in the browser; the reducer FSM handles all 5 actions per D-15/D-16/D-19; curl confirms `Content-Type: text/event-stream` and 4 stage + 1 result events; all reducer tests pass; typecheck + build succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SharePayloadSchema wrapper + encode/decode (W6 — stable wire format from first deploy) + clipboard markdown/plaintext + round-trip Vitest tests</name>
  <files>lib/schema/workout.ts, lib/share/encode.ts, lib/share/decode.ts, lib/clipboard/markdown.ts, lib/clipboard/plaintext.ts, tests/share-url-roundtrip.test.ts, tests/clipboard.test.ts</files>
  <behavior>
    - `SharePayloadSchema.parse({ workout: <valid Workout>, stripped: [] })` succeeds.
    - `SharePayloadSchema.parse({ workout: <valid Workout> })` succeeds with `stripped` defaulting to `[]` (D-17 zod default; W6 — the default-empty array means Plan 01-02 deploy URLs are valid input shapes for Plan 01-04's decoder).
    - `encodeShareUrl(dumbbellLegDay)` returns `{ encoded: string, stripped: [] }` — Plan 01-02 always returns `stripped: []` (the strip-chain loop body is a no-op TODO comment; Plan 01-04 implements the chain).
    - `decodeShareUrl(encodeShareUrl(workout).encoded)` returns `{ workout: <equal to input>, stripped: [] }` (round-trip preserves all fields including nulls and empty arrays via the SharePayloadSchema wrapper).
    - `decodeShareUrl("tampered-base64-garbage")` throws `Error("Invalid share link")`.
    - `decodeShareUrl(<lz-string-encoded payload with schema_version: "999">)` throws an error whose message contains "newer version of Exercised" (D-18).
    - `decodeShareUrl(<json longer than 50,000 chars>)` throws (DoS sanity cap, T-02-01).
    - `workoutToMarkdown(workout)` produces a string containing `# {workout_title}`, the creator handle, the difficulty + duration on a metadata line, each exercise as `## {exercise_name}` followed by a `{sets} × {reps} @ {rest_seconds}s rest` line and a bullet list of `form_cues`. Supersets render as `## Superset` heading + indented nested exercises + a shared rest line.
    - `workoutToPlainText(workout)` produces a string with no markdown markers: title on first line, creator on second, blank line, then numbered exercise list `1. {name} — {sets} × {reps} @ {rest_seconds}s rest`, form cues as indented `- {cue}` lines. Suitable for Notes / WhatsApp paste.
  </behavior>
  <read_first>
    - lib/schema/workout.ts (from Plan 01-01 — for WorkoutSchema; this task EXTENDS it with SharePayloadSchema)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Code Examples / Share-URL Encode/Decode" + §"Common Pitfalls / Pitfall 4"
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md §"Routing & Share Semantics" (D-16, D-17, D-18 — note D-17 strip chain ships in Plan 01-04, NOT this plan)
    - tests/fixtures/dumbbell-leg-day.json (the input shape these modules consume)
  </read_first>
  <action>
**SharePayloadSchema wrapper (W6 — defined upfront so wire format is stable from first deploy).** In `lib/schema/workout.ts`, add and export:
```ts
export const StripFieldSchema = z.enum(["sourceQuote", "form_cues", "equipment"]);
export const SharePayloadSchema = z.object({
  workout: WorkoutSchema,
  stripped: z.array(StripFieldSchema).default([]),
});
export type StripField = z.infer<typeof StripFieldSchema>;
export type SharePayload = z.infer<typeof SharePayloadSchema>;
```
The `stripped: z.array(...).default([])` field means a Plan 01-02 encoded URL (which always has `stripped: []`) decodes correctly under Plan 01-04's same schema; equally, a Plan 01-04 encoded URL (which may have `stripped: ["sourceQuote"]`) decodes correctly under Plan 01-02's same schema. The wire format is stable across the Phase 1 release — no in-flight share URLs ever break.

**Author `lib/share/encode.ts`** per RESEARCH §"Code Examples / Share-URL Encode/Decode", BUT in Plan 01-02 ship the wrapper-encoded shape with a no-op strip-chain placeholder (per W6 — Plan 01-04 fills the loop body, not the wire format):
```ts
import LZString from "lz-string";
import type { Workout } from "@/lib/schema/workout";
import { SharePayload, StripField } from "@/lib/schema/workout";

const STRIP_ORDER: StripField[] = ["sourceQuote", "form_cues", "equipment"];
const MAX_PAYLOAD_BYTES = 2048;

export function encodeShareUrl(workout: Workout): { encoded: string; stripped: StripField[] } {
  // TODO(Plan 01-04): implement the D-17 strip chain loop here. For Plan 01-02,
  // we wrap the workout in SharePayloadSchema with stripped:[] and ship — the
  // dumbbell baseline fixture fits comfortably under 2KB so no stripping needed.
  const stripped: StripField[] = [];
  const payload: SharePayload = { workout, stripped };
  const json = JSON.stringify(payload);
  const encoded = LZString.compressToEncodedURIComponent(json);
  return { encoded, stripped };
}
```
Plan 01-04 will replace the body with a loop over `STRIP_ORDER` that calls a `stripField(candidate, field)` helper between size checks. The exported type signature is identical (`{ encoded: string; stripped: StripField[] }`) so callers (ActionBar) don't change.

**Author `lib/share/decode.ts`** VERBATIM from RESEARCH §"Code Examples / Share-URL Encode/Decode" using the `SharePayloadSchema` wrapper from day one (W6 — no Plan 01-04 schema change needed):
```ts
import LZString from "lz-string";
import { SharePayloadSchema, StripField, type Workout } from "@/lib/schema/workout";

export function decodeShareUrl(encoded: string): { workout: Workout; stripped: StripField[] } {
  const json = LZString.decompressFromEncodedURIComponent(encoded);
  if (!json) throw new Error("Invalid share link");
  if (json.length > 50_000) throw new Error("Share link payload too large");
  let parsed: unknown;
  try { parsed = JSON.parse(json); } catch { throw new Error("Invalid share link payload"); }

  const result = SharePayloadSchema.safeParse(parsed);
  if (result.success) {
    return { workout: result.data.workout, stripped: result.data.stripped };
  }

  // D-18 schema-version mismatch — look for any issue whose path mentions schema_version
  const versionIssue = result.error.issues.find(i =>
    i.path.join(".") === "workout.schema_version" || i.path[0] === "schema_version"
  );
  if (versionIssue) {
    throw new Error("This share link was created with a newer version of Exercised — try pasting the original YouTube URL instead.");
  }
  throw new Error("Share link payload failed validation");
}
```
Add the 50,000 cap (T-02-01 DoS mitigation) BEFORE JSON.parse so an oversized lz-string-bomb never reaches the parser.

**Author `tests/share-url-roundtrip.test.ts`** with all 7 behaviors above as named Vitest cases:
1. Round-trip equality for dumbbellLegDay fixture (encode → decode → deep equal to input + `stripped: []`).
2. Decode rejects tampered-base64 with "Invalid share link" message.
3. Decode of a payload with `schema_version: "999"` throws with "newer version of Exercised" in the message (manually craft via `LZString.compressToEncodedURIComponent(JSON.stringify({ workout: {...fixture, schema_version: "999"}, stripped: [] }))`).
4. Decode of a json longer than 50,000 chars throws (manually craft a giant payload).
5. SharePayloadSchema.parse succeeds with explicit `stripped: []`.
6. SharePayloadSchema.parse succeeds without `stripped` (defaults to []).
7. Plan-forward sanity: decoding a `SharePayloadSchema`-shaped payload with a non-empty `stripped` array (e.g., `stripped: ["sourceQuote"]`) succeeds and the decoder surfaces that array — proves Plan 01-04's strip-chain output will decode under Plan 01-02's same decoder. (W6 acceptance.)

**Author `lib/clipboard/markdown.ts`** exporting `workoutToMarkdown(w: Workout): string`. Format spec: `# {workout_title}` + blank line + `by @{creator_username}` + ` · ` + `{difficulty}` + ` · ` + `~{estimated_duration_mins} min` + blank line + `Target muscles: {target_muscles.join(", ")}` + blank line + for each routine item: if `standard_set` → `## {exercise_name}` + `\n` + `` `{sets} × {reps} @ {rest_seconds}s rest` `` + form_cues as `- {cue}` bullets if non-empty; if `superset` → `## Superset · rest {rest_seconds}s` + for each inner exercise an indented sub-section. End with footer `---` + AI-disclaimer line (UI-SPEC §7.3 copy). SHRE-01 deliverable.

**Author `lib/clipboard/plaintext.ts`** exporting `workoutToPlainText(w: Workout): string`. Format: title on line 1, `by @{handle}` on line 2, blank line, `{difficulty} · ~{mins} min` line, blank, target muscles line, blank, then numbered exercise list — `1. {name}\n    {sets} × {reps} @ {rest_seconds}s rest\n    Form cues:\n        - {cue}\n        - {cue}\n`. Supersets render as `1. Superset (rest {N}s)\n    a) {inner_name} — {sets} × {reps}\n    b) {inner_name} — {sets} × {reps}\n`. End with blank line + AI-disclaimer. SHRE-02 deliverable.

**Author `tests/clipboard.test.ts`** with cases: (a) markdown contains `# Dumbbell-Only Leg Day` and `## Superset` and a `3 ×` substring; (b) plaintext contains `1. ` and an `a) ` superset inner marker; (c) markdown of an empty-form-cues exercise contains the exercise heading but no orphaned bullet character; (d) both functions are pure (calling twice with same input returns identical output).
  </action>
  <verify>
    <automated>pnpm test -- share-url-roundtrip clipboard && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `tests/share-url-roundtrip.test.ts` has ≥ 7 named test cases — all pass.
    - `tests/clipboard.test.ts` has ≥ 4 named test cases — all pass.
    - `lib/schema/workout.ts` now exports `SharePayloadSchema`, `StripFieldSchema`, `StripField`, `SharePayload`. Verify: `grep -c "SharePayloadSchema\|StripFieldSchema" lib/schema/workout.ts` ≥ 2.
    - `lib/share/encode.ts` uses the `SharePayloadSchema`-shaped wrapper (encoded JSON is `{ workout, stripped }` not bare workout). Verify: `grep -c "SharePayload\|stripped" lib/share/encode.ts` ≥ 2.
    - `lib/share/decode.ts` uses `SharePayloadSchema.safeParse`. Verify: `grep -c "SharePayloadSchema.safeParse" lib/share/decode.ts` ≥ 1.
    - `lib/share/decode.ts` contains BOTH the `> 50_000` length check AND the schema_version error message string. Verify: `grep -c "50_000\|50000" lib/share/decode.ts` ≥ 1; `grep -c "newer version of Exercised" lib/share/decode.ts` ≥ 1.
    - `lib/share/encode.ts` includes a `TODO(Plan 01-04)` comment (signpost so Plan 01-04 executor finds the right spot). Verify: `grep -c "TODO(Plan 01-04)" lib/share/encode.ts` ≥ 1.
    - `pnpm typecheck` exits 0; `pnpm build` exits 0.
  </acceptance_criteria>
  <done>The `SharePayloadSchema` wrapper is defined upfront (W6 — stable wire format from first deploy); encode/decode round-trip works against the wrapper; clipboard exporters produce correct markdown / plaintext; all unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WorkoutView render tree (header + cards + supersets) with Motion cascade + ActionBar with Copy MD / Copy Plain / Share / Watch on YouTube + chips/pills</name>
  <files>components/workout/WorkoutView.tsx, components/workout/WorkoutHeader.tsx, components/workout/ExerciseCard.tsx, components/workout/SupersetCard.tsx, components/workout/MusclePill.tsx, components/workout/DifficultyChip.tsx, components/workout/DurationChip.tsx, components/workout/ActionBar.tsx</files>
  <behavior>
    - WorkoutView renders an `<article>` with the workout title (Display size 36px mobile / 48px desktop), creator handle in muted Label, duration chip, difficulty chip, target-muscle pills, then a list of ExerciseCards and SupersetCards in the order specified by `workout.routine` (per D-08 all-at-once with staggered cascade — header first, then exercise cards cascade in 50–80ms stagger; no progressive per-exercise streaming).
    - When `shouldAnimateIn={true}` and `useReducedMotion()` is false: the workout container animates in via Motion stagger (header first, then 65ms stagger between subsequent cards), spring `{ damping: 22, stiffness: 240 }` per UI-SPEC §6.2.
    - When `shouldAnimateIn={false}` (share-link hydration per D-16): cards render instantly with no entrance animation (UI-SPEC §11 visual continuity rule).
    - When `useReducedMotion()` is true: cards cross-fade in over 200ms with no transform and no stagger (D-05 reduced-motion compliance).
    - The superset card has a visually-bracketed grouping with a 1px `--color-accent` left border indicating the group, and a single shared rest indicator per OUTV-04.
    - Clicking "Copy as Markdown" calls `navigator.clipboard.writeText(workoutToMarkdown(workout))` then fires sonner `toast.success('Copied to clipboard')` (SHRE-01).
    - Clicking "Copy as Plain Text" same flow with the plaintext variant (SHRE-02).
    - Clicking "Share Workout" calls `encodeShareUrl(workout)`, builds `${window.location.origin}/?w=${encoded}`, writes to clipboard, fires sonner `toast.success('Share link copied')` (SHRE-03).
    - The "Watch on YouTube" button is an `<a target="_blank" rel="noopener noreferrer">` with the lucide `ExternalLink` icon (OUTV-05); Plan 01-02 derives a placeholder watch URL from the creator handle (`https://youtube.com/@{creator_username}`) since `startTimestamp` rendering is deferred to Phase 4 polish.
    - All four ActionBar buttons are ≥ 44px height (OUTV-07 — buttons use `size="lg"` h-11 minimum). Inline on desktop only in Plan 01-02; sticky-bottom mobile variant ships in Plan 01-05.
  </behavior>
  <read_first>
    - lib/schema/workout.ts (from Plan 01-01 + extended in Plan 01-02 Task 2 — Workout type for prop typing)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §6.2 Result reveal + §6.3 Card hover + §6.4 Form-cue expansion + §7.3 Success state copy + §8.2 Component inventory + §8.3 Component contracts + §9.4 Success composition + §11 Share Link Visual Contract
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Code Examples / Motion 12 Result Cascade"
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md §"Routing & Share Semantics" (D-16 share-link hydration affects shouldAnimateIn)
    - tests/fixtures/dumbbell-leg-day.json (the input shape these components consume)
    - lib/clipboard/markdown.ts + lib/clipboard/plaintext.ts (from Task 2)
    - lib/share/encode.ts (from Task 2)
    - components/extract/ExtractFlow.tsx (caller — passes `workout` and `shouldAnimateIn`)
  </read_first>
  <action>
**Author `components/workout/MusclePill.tsx`** as `"use client"` — small chip with `--color-surface-glass` background, 1px `--color-border-glass` border, 12px border-radius (smaller than glass-card's 16px since pills are small), 4px icon-to-text gap (UI-SPEC §2 2xs token), padding `px-3 py-1`, Label-size type (14px/500), `--color-text-primary` label, optional lucide icon (no icon in Plan 01-02 — labels only).

**Author `components/workout/DifficultyChip.tsx`** as `"use client"` — accepts `{ difficulty: "beginner" | "intermediate" | "advanced" }`. UI-SPEC §4.1 rule: `beginner` uses `--color-text-muted` border + label; `intermediate` and `advanced` use `--color-accent` border + label (the chip is the accent-application path for difficulty — D-01 single accent across CTAs / focus rings / active stage indicators / difficulty chip). Same shape/size as MusclePill. Label is title-cased on render. Note: Plan 01-02 only renders `intermediate` (the dumbbell-leg-day fixture's difficulty); Plan 01-03's fixture variety exercises all 3 difficulty values.

**Author `components/workout/DurationChip.tsx`** as `"use client"` — lucide `Clock` icon (size 14, `aria-hidden="true"`) + label `~{mins} min` in Geist Sans Label-size. Same chip shape as MusclePill.

**Author `components/workout/WorkoutHeader.tsx`** as `"use client"` — renders a glass-card containing: workout_title in Display size (36px mobile / 48px desktop via `text-[36px] md:text-[48px]`, weight 600, letter-spacing -0.02em), `by @{creator_username}` in muted Label below, then a flex-row of `<DurationChip />` + `<DifficultyChip />` + `target_muscles.map(m => <MusclePill key={m} muscle={m} />)`. Gap-3 between chips. `lg`-size padding inside the card.

**Author `components/workout/ExerciseCard.tsx`** as `"use client"` — accepts `{ exercise: StandardSet; index: number; shouldAnimateIn: boolean }` (UI-SPEC §8.3). Renders a glass-card with: exercise_name in Heading size (22px/600), sets-reps-rest line in Geist Mono `font-mono tabular-nums` 16px/500 ("3 × 12 @ 60s rest"), then a Motion-animated form-cues expand toggle if `form_cues.length > 0`. The toggle is a `<button aria-expanded={isOpen}>` with label `Form cues ({n})` collapsed / `Hide form cues` expanded (UI-SPEC §7.3 discretion copy). On expand, the bullet list renders below using Motion `<motion.div animate={{ height: isOpen ? 'auto' : 0 }}>` with `cubic-bezier(0.22, 1, 0.36, 1)` over 200ms and ChevronDown icon rotating 0→180° same easing (UI-SPEC §6.4). Under `useReducedMotion()` true, toggle is instant with no rotation (D-05). Touch-target ≥ 44px on the toggle button. Plan 01-02 omits the `startTimestamp`/`sourceQuote`/`equipment` rendering (Phase 4 polish per CONTEXT.md `<deferred>`); schema fields are accepted but not rendered.

**Author `components/workout/SupersetCard.tsx`** as `"use client"` — accepts `{ superset: Superset; index: number; shouldAnimateIn: boolean }`. Renders a glass-card with a Label-size header `Superset · rest {rest_seconds}s` (UI-SPEC §7.3 verbatim). Inside, a left-border bracket visual (1px `--color-accent` left border on a nested container indicating grouping) containing each inner exercise rendered as a stripped-down ExerciseCard inner (no outer glass — they're inside the superset's glass) showing name + sets/reps line + form-cue toggle. Single shared rest indicator is the header's "rest {N}s" portion (OUTV-04 supersets visually bracketed with shared rest).

**Author `components/workout/ActionBar.tsx`** as `"use client"` — accepts `{ workout: Workout }`. Renders a flex row of four buttons:
1. `<Button variant="outline" size="lg" asChild>` wrapping an `<a href={`https://youtube.com/@${workout.creator_username}`} target="_blank" rel="noopener noreferrer">` with `Watch on YouTube` label + lucide `ExternalLink` icon (OUTV-05).
2. `<Button variant="outline" size="lg">Copy as Markdown</Button>` — onClick calls `navigator.clipboard.writeText(workoutToMarkdown(workout))` then fires `toast.success('Copied to clipboard')` from sonner (SHRE-01).
3. `<Button variant="outline" size="lg">Copy as Plain Text</Button>` — same flow (SHRE-02).
4. `<Button variant="default" size="lg">Share Workout</Button>` with lucide `Link2` icon — onClick: `const { encoded } = encodeShareUrl(workout); const url = `${window.location.origin}/?w=${encoded}`; navigator.clipboard.writeText(url); toast.success('Share link copied')` (SHRE-03).

All four buttons must be ≥ 44px height (OUTV-07). Plan 01-02 ships ActionBar inline on desktop only; sticky-bottom mobile variant + tooltip disambiguation + full responsive layout ship in Plan 01-05.

**Author `components/workout/WorkoutView.tsx`** per RESEARCH §"Code Examples / Motion 12 Result Cascade": accepts `{ workout: Workout; shouldAnimateIn: boolean }`. `useReducedMotion()` from `motion/react`. Define `containerVariants` with `staggerChildren: 0.065` (UI-SPEC §6.2 65ms — within the D-08 50–80ms range), `headerVariants` and `cardVariants` with `transition: { type: "spring", damping: 22, stiffness: 240 }` per UI-SPEC §6.2 settled-value. Compute `animate` and `initial` per the §6.2 rule: when reduced motion OR `!shouldAnimateIn`, set both to `false` (Motion treats `false` as "no animation"). When animating: `initial="hidden"`, `animate="visible"`. Render `<motion.article variants={containerVariants}>` containing `<motion.div variants={headerVariants}><WorkoutHeader workout={workout} /></motion.div>` then `workout.routine.map((item, i) => <motion.div key={i} variants={cardVariants}>{ item.type === 'standard_set' ? <ExerciseCard exercise={item} index={i} shouldAnimateIn={shouldAnimateIn} /> : <SupersetCard superset={item} index={i} shouldAnimateIn={shouldAnimateIn} /> }</motion.div>)` and `<ActionBar workout={workout} />` after the routine. Container has `max-w-3xl mx-auto px-6 pt-12 space-y-6` per UI-SPEC §9.4 desktop layout.

Update `components/extract/ExtractFlow.tsx` from Task 1 — the `success` branch now renders `<WorkoutView workout={state.workout} shouldAnimateIn={!state.fromShareLink} />` instead of the placeholder.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `components/workout/WorkoutView.tsx` uses Motion `useReducedMotion()` and stagger. Verify: `grep -c "useReducedMotion" components/workout/WorkoutView.tsx` ≥ 1; `grep -c "staggerChildren" components/workout/WorkoutView.tsx` ≥ 1; `grep -cE "0.065|65" components/workout/WorkoutView.tsx` ≥ 1.
    - Source assertion: `components/workout/WorkoutView.tsx` uses spring with damping 22 + stiffness 240. Verify: `grep -c "damping: 22" components/workout/WorkoutView.tsx` ≥ 1; `grep -c "stiffness: 240" components/workout/WorkoutView.tsx` ≥ 1.
    - Source assertion: `components/workout/SupersetCard.tsx` renders the verbatim `Superset · rest` label per UI-SPEC §7.3. Verify: `grep -c 'Superset · rest' components/workout/SupersetCard.tsx` ≥ 1.
    - Source assertion: `components/workout/ExerciseCard.tsx` renders sets/reps in Geist Mono with tabular-nums. Verify: `grep -cE 'font-mono|GeistMono|tabular-nums' components/workout/ExerciseCard.tsx` ≥ 1.
    - Source assertion: `components/workout/ActionBar.tsx` calls all four expected functions/handlers. Verify: `grep -cE "workoutToMarkdown|workoutToPlainText|encodeShareUrl|toast.success" components/workout/ActionBar.tsx` ≥ 4.
    - Source assertion: `components/workout/ActionBar.tsx` includes `Watch on YouTube` label and `ExternalLink` icon (OUTV-05). Verify: `grep -c "Watch on YouTube" components/workout/ActionBar.tsx` ≥ 1; `grep -c "ExternalLink" components/workout/ActionBar.tsx` ≥ 1.
    - Source assertion: `components/extract/ExtractFlow.tsx` passes `shouldAnimateIn={!state.fromShareLink}` to WorkoutView. Verify: `grep -cE 'shouldAnimateIn=.\!state\.fromShareLink' components/extract/ExtractFlow.tsx` ≥ 1.
    - All four ActionBar buttons are ≥ 44px height. Verify: `grep -cE 'size="lg"|h-11|h-14' components/workout/ActionBar.tsx` ≥ 1.
    - `pnpm typecheck` exits 0; `pnpm build` exits 0; all prior Vitest tests still pass.
  </acceptance_criteria>
  <done>Running `pnpm dev` and pasting a YouTube URL renders the Dumbbell-Only Leg Day fixture in glass cards with cascade animation; clicking Copy as Markdown / Copy as Plain Text / Share Workout copies the expected content with a toast confirmation; opening the share URL in another tab renders the same workout instantly (no cascade) — wire format is the SharePayloadSchema wrapper, stable across Phase 1; all unit tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify the local end-to-end happy path</name>
  <what-built>
    A locally-running mock-deployable Exercised demo. From a single browser session, the user can:
    1. Open `http://localhost:3000` and see the dark ambient gradient background with green/violet/coral orbs drifting, a centered hero with "Turn workout videos into structured routines." headline, a glass-styled URL input, and a large green "Extract Workout" CTA.
    2. Paste any YouTube URL (e.g. `https://www.youtube.com/watch?v=dQw4w9WgXcQ`) and click the CTA (or press Enter).
    3. Watch the hero transition to a loading view that shows the four stage labels in sequence ("Fetching video data…" → "Transcribing audio…" → "Analyzing form cues…" → "Generating routine…") with pulse-dot on the active stage and checkmarks accumulating, with 4 skeleton workout cards visible below, over ~4–5 seconds total.
    4. See the loading view dissolve and the workout render in glass cards with a staggered cascade (header first, then exercise cards 65ms apart, plus the bracketed superset). Title in Display Geist, sets/reps in Geist Mono.
    5. Click "Copy as Markdown" → see "Copied to clipboard" toast → paste into another app and confirm a sane markdown representation.
    6. Click "Copy as Plain Text" → same flow with the plaintext variant.
    7. Click "Share Workout" → see "Share link copied" toast → open the copied URL in a new tab → confirm the same workout view renders instantly (no cascade animation).
  </what-built>
  <how-to-verify>
    1. From the repo root, run `pnpm install --frozen-lockfile && pnpm dev`. Confirm dev server starts on http://localhost:3000 with no console errors.
    2. Open Chrome (or Safari/Firefox), navigate to http://localhost:3000. Confirm: dark background, 3 drifting orbs visible behind the hero, centered headline reads "Turn workout videos into structured routines.", input placeholder reads "Paste a YouTube workout video URL", and the green "Extract Workout" button is visually prominent. Open DevTools → Console; confirm 0 errors and 0 hydration warnings.
    3. Open DevTools → Network tab and filter for `extract`. Paste `https://www.youtube.com/watch?v=dQw4w9WgXcQ` and click Extract Workout (or press Enter). Confirm: a POST request to `/api/extract` shows in the Network tab with response Type `eventstream` (or `text/event-stream`); the request stays in "pending" state with chunked data arriving over ~5 seconds.
    4. While loading, watch the stages: the first row shows pulse-dot beside "Fetching video data…", then progressively the dot becomes a checkmark and the next row activates. Four skeleton cards are visible below the stage list. Total duration ~4–5s. Confirm no `setTimeout`-driven stage changes (the network tab event timestamps should be ~1.0–1.2s apart).
    5. After "Generating routine…" completes, the loading view dissolves and the workout renders: title "Dumbbell-Only Leg Day · Hypertrophy Block" (or whatever the fixture defines), creator handle "by @kynanfit" (or whichever synthetic handle the Plan 01-01 SUMMARY documents), duration chip "~45 min", difficulty chip "Intermediate" in neon green, target-muscle pills (Quads / Glutes / etc.), exercise cards, AND a visually-bracketed superset card. Cards animate in with a staggered cascade.
    6. On an exercise card with form cues, click the "Form cues (N)" toggle — the bullet list expands smoothly with the chevron rotating. Click again — collapses.
    7. Click "Copy as Markdown" → confirm sonner toast "Copied to clipboard" appears bottom-center. Open a text editor (e.g., Notes), paste, confirm the output begins with `# Dumbbell-Only Leg Day` and contains `## Superset` somewhere.
    8. Click "Copy as Plain Text" → same toast → paste into Notes → confirm a numbered exercise list with `1. ` prefixes and no `#` markdown characters.
    9. Click "Share Workout" → confirm toast "Share link copied" → check the URL bar; the copied URL should start with `http://localhost:3000/?w=` and have a long compressed payload after.
    10. Open the copied URL in a NEW browser tab (or incognito window). Confirm: the workout view renders immediately (no loading stages, no cascade animation — instant). All content identical to the original render.
    11. macOS only: enable System Settings → Accessibility → Display → Reduce Motion. Reload http://localhost:3000 and paste a URL again. Confirm: gradient orbs are static (no drift), pulse-dot is static, skeleton shimmer is static, cascade is replaced by a simultaneous cross-fade with no transform/stagger (D-05 reduced-motion compliance — full per-moment refinement ships in Plan 01-05).
    12. Open DevTools → Lighthouse → run Performance audit. Confirm CLS = 0 (Geist zero-CLS verified per A2).
    13. Resize Chrome window below 768px width. Confirm the hero reflows: headline shrinks to 36px, layout stays single-column, no horizontal scroll, content remains readable.
    14. Run `curl -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' -d '{}'` and confirm HTTP 400 with `{"error":"Invalid request","issues":[...]}` body.
    15. Run `curl -X POST http://localhost:3000/api/extract -H 'Content-Type: application/json' --data-raw 'not-json'` and confirm HTTP 400 with `{"error":"Malformed JSON body"}` body.
  </how-to-verify>
  <files>(no file modifications — this task is a human verification checkpoint against the running local dev server from Tasks 1–3)</files>
  <action>Pause execution for the human reviewer to walk through all 15 steps in <how-to-verify> against the locally-running `pnpm dev` server. The executor must NOT modify any code during this task — its job is to wait for the resume-signal. If the human reports issues, the executor diagnoses (re-reads the relevant Task 1/2/3 source files based on which step failed) and proposes targeted fixes before re-requesting verification.</action>
  <verify>Human-only verification per <how-to-verify> steps 1–15. No automated check applicable — automated verification ran in Tasks 1–3 (`pnpm test`, `pnpm typecheck`, `pnpm build`, curl SSE smoke).</verify>
  <done>Human reviewer types "approved" after all 15 steps in <how-to-verify> pass cleanly on the local dev server.</done>
  <resume-signal>Type "approved" if all 15 steps pass cleanly. Otherwise describe specific issue (which step, what was wrong) and the executor will diagnose and fix before re-requesting verification.</resume-signal>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 5: Deploy to Vercel + verify production share-link round-trip + commit + STATE.md update</name>
  <files>vercel.json (verify), .env.example (verify), README.md, .planning/STATE.md</files>
  <read_first>
    - vercel.json (from Plan 01-01)
    - .env.example (from Plan 01-01)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Open Questions / Q3" (X-Accel-Buffering placement) + §"Common Pitfalls / Pitfall 1" (Vercel buffering)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-SKELETON.md (architectural decisions to honor)
  </read_first>
  <action>
Author a minimal `README.md` at repo root containing: project description (1 paragraph from PROJECT.md), `## Local development` section with the exact commands (`pnpm install`, `pnpm dev` opening http://localhost:3000), `## Deployment` section noting Vercel auto-deploys from main and listing the required env var `EXTRACT_MODE=mock`, and `## Architecture` section linking to `.planning/phases/01-mock-deployable-premium-ui-demo/01-SKELETON.md` for the locked architectural decisions.

Commit all Plan 01-02 changes via a single conventional commit `feat(01): walking skeleton UI — ExtractFlow + WorkoutView + ActionBar + share encode/decode + clipboard` (and the standard Co-Authored-By line). Push to `main` (or to the user's chosen deploy branch — confirm via checkpoint sub-message).

After push, monitor the Vercel deploy via `gh run watch` or the Vercel dashboard URL. Once deploy succeeds, the user provides the production URL (e.g., `https://exercised.vercel.app` or `https://exercised-{hash}.vercel.app`). Pause for the user to provide the deployed URL.

With the deployed URL `$PROD_URL` in hand, run the production verification suite via the executor's Bash tool:
1. `curl -sI $PROD_URL` — confirm HTTP 200 and `Content-Security-Policy` not blocking; capture `X-Frame-Options` header (default Vercel sends SAMEORIGIN — T-02-08 disposition confirmed).
2. `curl -sN -X POST $PROD_URL/api/extract -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' --max-time 8` — confirm streaming response arrives in chunks (not all at once — Pitfall 1 verification), and contains 4 `"type":"stage"` events plus 1 `"type":"result"` event. If all events arrive in one chunk, Vercel buffering is broken; revisit headers in `app/api/extract/route.ts`.
3. `curl -sI $PROD_URL/api/extract -X POST -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}'` — capture and confirm response headers: `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `X-Accel-Buffering: no`. Each header confirms one PIPE-01 requirement and the four-header Pitfall 1 mitigation.
4. `curl -X POST $PROD_URL/api/extract -d '{}' -H 'Content-Type: application/json'` — confirm 400 status (T-01-01 mitigation live in prod).

Then a HUMAN CHECKPOINT GATE: ask the user to (a) open $PROD_URL in their browser, paste any YouTube URL, confirm the full happy path works identically to local Task 4 step 5, (b) generate a Share Workout URL, (c) open the share URL in a DIFFERENT browser or device (e.g., phone) and confirm the workout renders identically with no SSE stream. This is the OPS-02 acceptance gate ("Mock-mode demo is deployed and shareable from the end of Phase 1"). Resume signal: user replies "prod verified" with the production URL pasted in the message so it goes into the commit log.

Update `.planning/STATE.md` `stopped_at` line and `last_activity` line to reflect "Phase 1 Plans 01-01 + 01-02 (Walking Skeleton) shipped to Vercel at $PROD_URL". Commit with `chore(state): record Walking Skeleton ship to $PROD_URL`.
  </action>
  <verify>
    <automated>test -n "$PROD_URL" && curl -sI "$PROD_URL" | head -1 | grep -q "200" && curl -sN -X POST "$PROD_URL/api/extract" -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' --max-time 8 > /tmp/prod-sse.log && stage_count=$(grep -c '^data:.*"type":"stage"' /tmp/prod-sse.log || echo 0); result_count=$(grep -c '^data:.*"type":"result"' /tmp/prod-sse.log || echo 0); test "$stage_count" -eq 4 && test "$result_count" -eq 1 && curl -sI -X POST "$PROD_URL/api/extract" -H 'Content-Type: application/json' -d '{"url":"https://www.youtube.com/watch?v=dQw4w9WgXcQ"}' | grep -qi "x-accel-buffering: no"</automated>
  </verify>
  <acceptance_criteria>
    - Live Vercel URL `$PROD_URL` returns HTTP 200 on `GET /`.
    - `curl -sI -X POST $PROD_URL/api/extract -H 'Content-Type: application/json' -d '{"url":"..."}'` returns headers `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `X-Accel-Buffering: no` (Pitfall 1 mitigation live in prod).
    - Streaming response from `$PROD_URL/api/extract` arrives in 5 separate chunks (4 stage + 1 result) — NOT one buffered chunk. This proves Vercel's edge isn't buffering and the streaming architecture works in production. Verify via `curl -sN ... | tee /tmp/log` and confirm timestamps on lines are spaced ~1s apart (not all at the end).
    - `curl -X POST $PROD_URL/api/extract -d '{}' -H 'Content-Type: application/json'` returns HTTP 400 with `{ error: "Invalid request", issues: [...] }`.
    - User confirms via the human checkpoint: paste-URL → SSE → workout-renders works in production browser; share-link generated on $PROD_URL opens correctly in a SECOND browser/device (this is the OPS-02 success-criterion-#1 ROADMAP gate).
    - Vercel dashboard shows one-click rollback option for the deploy (OPS-01 ack).
    - Vercel project env vars contain `EXTRACT_MODE=mock` for Preview AND Production (verifiable via Vercel dashboard or `vercel env ls`).
    - `.planning/STATE.md` updated with the production URL in `stopped_at` and `last_activity`.
    - `README.md` exists at repo root with `## Local development`, `## Deployment`, `## Architecture` sections.
    - `git log --oneline -2` shows the two commits: `feat(01): walking skeleton UI — ExtractFlow + WorkoutView + ActionBar + share encode/decode + clipboard` and `chore(state): record Walking Skeleton ship to ...`.
  </acceptance_criteria>
  <done>A publicly accessible Vercel URL serves the Walking Skeleton; pasting any YouTube URL into it produces the SSE-driven stage cascade and rendered workout in glass cards; the Share Workout button generates a URL that opens correctly in a second browser/device with no backend round-trip; Vercel one-click rollback is available; STATE.md records the production URL.</done>
</task>

</tasks>

<verification>
  After Task 5 the following must all hold:
  - `pnpm install --frozen-lockfile && pnpm test && pnpm typecheck && pnpm build` exits 0 across all four commands.
  - `grep -c "SharePayloadSchema" lib/schema/workout.ts` ≥ 1 (W6 — wire format defined upfront).
  - `grep -c "SharePayload" lib/share/encode.ts` ≥ 1 (encode against wrapper).
  - `grep -c "SharePayloadSchema.safeParse" lib/share/decode.ts` ≥ 1 (decode against wrapper).
  - `grep -c "useReducedMotion" components/workout/WorkoutView.tsx` ≥ 1 (DSGN-05 prep — full per-moment in Plan 01-05).
  - `grep -c "staggerChildren" components/workout/WorkoutView.tsx` ≥ 1 (UI-SPEC §6.2 cascade).
  - Live production URL (`$PROD_URL`) returns streamed SSE (4 stage + 1 result events) under ~8s for the happy path.
  - Human confirms share-link works cross-browser/cross-device on the deployed URL (OPS-02 acceptance gate).
</verification>

<success_criteria>
  Plan 01-02 is complete when the user can open the public Vercel URL, paste any YouTube URL, watch the 4-stage SSE cascade animate over skeleton cards for ~4–5s, see the Dumbbell-Only Leg Day fixture render in glass cards with the Motion staggered cascade, copy the workout as Markdown or Plain Text (both via clipboard with a toast confirmation), and generate a Share Workout link that opens correctly in a second browser/device — all without any backend round-trip on the share-link path.

  The architectural decisions in `01-SKELETON.md` are locked: schema at `lib/schema/workout.ts` with `schema_version: "1"` + SharePayloadSchema wrapper (W6), single `/api/extract` route gated by `EXTRACT_MODE` env var, `useReducer` state machine in the single Client island `<ExtractFlow />`, Tailwind 4 `@theme` block in `app/globals.css` as the only design-token location, Geist via `geist@1.x` npm with zero CLS, Motion 12 cascade with spring `{ damping: 22, stiffness: 240 }` and 65ms stagger, lz-string `compressToEncodedURIComponent` for share URLs, deployed to Vercel from `main` with Fluid Compute defaults.

  Wire format (W6 critical): share URLs minted in Plan 01-02 will decode correctly under Plan 01-04's same `SharePayloadSchema` — the only Plan 01-04 change is internal strip-chain logic, never the wire shape. No in-flight share URLs break during the Phase 1 release.
</success_criteria>

<output>
Create `.planning/phases/01-mock-deployable-premium-ui-demo/01-02-SUMMARY.md` when done, capturing:
- Production URL (`$PROD_URL`)
- Per-fixture compressed payload size (just for `dumbbell-leg-day.json` in this plan — full 5-fixture table ships in Plan 01-04)
- Confirmed-working SSE chunk count and timing observed in production curl
- Any Pitfall 1/8/9 anomalies surfaced during dev and how they were fixed
- The wire-format confirmation: `SharePayloadSchema`-wrapped payload encodes/decodes correctly, `stripped: []` no-op shipped (Plan 01-04 will fill the loop without changing the wrapper)
- A "Plan 01-03 hand-off" note: the four additional fixtures to add (bodyweight-push, full-body-2-supersets, warmup-3-exercises, hypertrophy-12-exercises), the URL-keyword error branches to wire into `MockExtractionService` (D-13), the full inline URL-validation error UI from UI-SPEC §7.1, and the ErrorState component for NETWORK/NO_WORKOUT/RATE_LIMITED variants.
</output>
</content>
</invoke>