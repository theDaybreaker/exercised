---
phase: 01-mock-deployable-premium-ui-demo
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - app/globals.css
  - components/workout/ActionBar.tsx
  - components/workout/ExerciseCard.tsx
  - components/extract/LoadingStages.tsx
  - components/layout/AmbientBackground.tsx
  - components/workout/SkeletonCard.tsx
  - components/workout/WorkoutView.tsx
  - components/workout/SupersetCard.tsx
  - .planning/STATE.md
autonomous: false
requirements:
  - DSGN-05
  - DSGN-06
  - OUTV-07

must_haves:
  truths:
    - "All 7 motion moments enumerated in UI-SPEC §6.5 comply with `prefers-reduced-motion: reduce` — toggle macOS Reduce Motion in System Settings, reload the deployed URL, and verify: gradient orbs static (no drift), pulse-dot static (no scale animation), skeleton shimmer static, result-cascade replaced by simultaneous cross-fade with no transform/stagger, card hover-lift disabled, form-cue expansion instant (no rotation, no height animation), toast entrance instant fade-only."
    - "On a mobile width (<768px), the workout view shows the ActionBar fixed-bottom with `safe-area-inset-bottom` padding for notched devices; the action bar remains thumb-reachable regardless of scroll position; the exercise list scrolls behind it."
    - "All interactive elements on mobile meet ≥ 44px touch target (OUTV-07): Extract Workout CTA (56px via `h-14`), Copy/Share/Watch buttons (≥ 44px via `h-11`), form-cue expand toggles (full card row width × 44px height), ErrorState recovery buttons (44px)."
    - "On the deployed Vercel URL, running `pnpm dlx @axe-core/cli` against the production gradient + glass cards reports zero `color-contrast` violations (DSGN-06)."
    - "On desktop, hovering over Copy as Markdown vs Copy as Plain Text shows a Radix tooltip disambiguating the two icons (both use `Clipboard` from lucide); on mobile, no tooltip appears (hover/pointer-fine media query) — the text labels carry the meaning."
    - "Sharing a workout from the deployed URL on one browser/device opens correctly in a SECOND browser/device with no SSE stream, no backend call, no cascade animation — the workout renders instantly with identical layout (this is the cross-device share-link smoke test, the OPS-02 + SHRE-03 acceptance gate for the whole phase)."
    - "Mobile glass fallback recipe (UI-SPEC §5.2) is active when `@supports not (backdrop-filter: blur(1px))` is true — verified via DevTools 'Emulate CSS feature' tooling or by adding `:has(.glass-card)` and Chrome's `Rendering` panel `prefers-reduced-motion: reduce` toggle."
    - "All Phase 1 requirement IDs are in 'Complete' status in `.planning/STATE.md` and the production demo URL is recorded as the Phase 1 ship artifact."
  artifacts:
    - path: "app/globals.css"
      provides: "Per-moment reduced-motion gating refinement: pulse-dot scale, shimmer sweep, orb drift, card-hover-lift, form-cue rotate/height — each has both an `@media (prefers-reduced-motion: reduce)` rule AND uses Motion's `useReducedMotion()` hook where JS-driven"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: "components/workout/ActionBar.tsx"
      provides: "Mobile sticky-bottom variant with safe-area-inset padding + responsive switch via Tailwind md: breakpoint"
      contains: "fixed bottom-0\\|safe-area-inset"
    - path: "tooltip-wrapping on Copy buttons in ActionBar.tsx"
      provides: "Radix tooltip via shadcn's <Tooltip>/<TooltipTrigger>/<TooltipContent> wrapping the two Clipboard-icon buttons; hover-only via media query"
  key_links:
    - from: "app/globals.css"
      to: "all 7 reduced-motion moments"
      via: "@media (prefers-reduced-motion: reduce) block listing each animated selector with `animation: none !important; transition: none !important; transform: none !important;`"
      pattern: "prefers-reduced-motion"
    - from: "components/workout/ActionBar.tsx"
      to: "mobile sticky-bottom"
      via: "Tailwind responsive classes — `md:relative md:bottom-auto md:bg-transparent` (desktop inline) + `fixed bottom-0 left-0 right-0 bg-[var(--color-surface-glass)] backdrop-blur-xl border-t border-white/10 pb-[env(safe-area-inset-bottom)]` (mobile sticky)"
      pattern: "fixed bottom-0|md:relative"
    - from: "components/workout/ActionBar.tsx tooltip"
      to: "Radix tooltip"
      via: "<Tooltip><TooltipTrigger asChild><Button>...</Button></TooltipTrigger><TooltipContent>...</TooltipContent></Tooltip>"
      pattern: "TooltipTrigger.*TooltipContent"
---

<objective>
Close the remaining Phase 1 polish gaps: full per-moment reduced-motion compliance across all 7 motion moments (UI-SPEC §6.5), mobile sticky-bottom ActionBar with safe-area padding (OUTV-07), desktop tooltip disambiguation on Copy buttons (UI-SPEC §8.1 / §14), mobile glassmorphism perf fallback (UI-SPEC §5.2 — already partially shipped via `@supports` in Plan 1, validate in this plan), and the deployed `axe-core` WCAG audit closing DSGN-06.

This plan is also the **Phase 1 exit gate** — the cross-browser/cross-device share-link smoke test from a SECOND device on the deployed URL is the OPS-02 + SHRE-03 acceptance check, and the `.planning/STATE.md` finalization marks the phase complete.

Purpose: Plans 1–3 shipped the architecture and the user-visible behaviors. Plan 4 is the quality bar — without it, the demo "works" but doesn't feel premium on accessibility-on, mobile, or to a third-party reviewer. Plans 1–3 are necessary; Plan 4 is what makes the demo presentation-ready.

Output: A Phase 1 ship-quality deployed demo. Every UI behavior promised in the ROADMAP success criteria is verifiably true on the production URL; every accessibility / mobile / reduced-motion path is covered; STATE.md records the phase ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-SKELETON.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-01-PLAN.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-02-PLAN.md
@.planning/phases/01-mock-deployable-premium-ui-demo/01-03-PLAN.md
@app/globals.css
@components/workout/ActionBar.tsx
@components/workout/ExerciseCard.tsx
@components/extract/LoadingStages.tsx
@components/layout/AmbientBackground.tsx
@components/workout/SkeletonCard.tsx

<interfaces>
<!-- The 7 motion moments from UI-SPEC §6.5 (the EXHAUSTIVE list — any new animation must add a reduced-motion fallback): -->
1. Gradient orb drift (CSS keyframes on `.orb-1`, `.orb-2`, `.orb-3` from app/globals.css)
2. Active-stage pulse-dot scale (CSS keyframes on `.pulse-dot`)
3. Skeleton shimmer sweep (CSS keyframes on `.skeleton-shimmer`)
4. Result-cascade stagger (Motion variants in WorkoutView.tsx — gated by `useReducedMotion()` from Plan 1)
5. Card hover-lift transform + box-shadow (CSS transition on `.card-hover-lift` or `:hover` selector)
6. Form-cue expansion (Motion `<motion.div animate={{ height }}>` + chevron rotate — already gated by `useReducedMotion()` from Plan 1's ExerciseCard, but reverify)
7. Toast entrance (sonner Toaster — surface a `richColors` + `theme="dark"` Toaster with `expand={false}` + reduced-motion respected by sonner's built-in handling; verify by inspecting sonner's animation classes)

<!-- Tooltip pattern (verbatim shadcn ui pattern): -->
```tsx
<Tooltip>
  <TooltipTrigger asChild>
    <Button>Copy as Markdown</Button>
  </TooltipTrigger>
  <TooltipContent className="hidden md:block">
    Copy a markdown representation of this workout
  </TooltipContent>
</Tooltip>
```
The `hidden md:block` Tailwind class on `<TooltipContent>` is the cleanest way to hide tooltips on mobile (where there's no hover anyway, and the text-label buttons already disambiguate without the tooltip).

<!-- Mobile sticky-bottom ActionBar (UI-SPEC §9.4 verbatim): -->
> Mobile: <ActionBar /> is sticky-bottom (fixed to viewport bottom edge, glass-treated, with safe-area-inset-bottom padding for notched devices)

Class set: `fixed bottom-0 left-0 right-0 z-30 bg-[var(--color-surface-glass)] backdrop-blur-xl border-t border-white/10 pb-[env(safe-area-inset-bottom)] px-6 py-3 flex gap-3 justify-around md:relative md:bottom-auto md:px-0 md:bg-transparent md:backdrop-blur-none md:border-0 md:pb-0 md:justify-end`

<!-- Glass perf fallback (Plan 1 shipped `@supports not (backdrop-filter)` block; Plan 4 adds the prefers-reduced-motion + low-memory branch from UI-SPEC §5.2): -->
Plan 1 produced:
```css
@supports not (backdrop-filter: blur(1px)) {
  .glass-card { background-color: rgba(20,22,28,0.85); backdrop-filter: none; }
}
```
Plan 4 adds an explicit "low-power preference" branch (acknowledging the validation deferral noted in CONTEXT.md `<deferred>`):
```css
/* Mobile glass perf fallback — applies when prefers-reduced-motion is on, which usually correlates with low-power preference */
@media (prefers-reduced-motion: reduce) and (max-width: 480px) {
  .glass-card {
    background-color: rgba(20, 22, 28, 0.85);
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }
}
```
This is a heuristic — real-device validation on mid-range Android is acknowledged as deferred to a future perf pass if BrowserStack flags it (STATE.md "Blockers/Concerns" + CONTEXT.md `<deferred>`).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new untrusted inputs in Plan 4) | This plan is pure polish — no new API surfaces, no new data inputs, no new env vars. Plan 1's T-01-* mitigations remain in force; Plan 2 + 3 added no new boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure (Accessibility) | `prefers-reduced-motion` may not toggle all 7 moments if any moment uses CSS animation outside the reduced-motion media block | mitigate | Task 1 verification systematically lists each of the 7 moments and inspects globals.css to confirm a corresponding `@media (prefers-reduced-motion: reduce) { … }` override exists for that moment's selector. Manual macOS Reduce Motion toggle test in Task 4 closes the loop. |
| T-04-02 | Information Disclosure | DSGN-06 contrast failure due to gradient-orb position shifting the effective backdrop luminance | mitigate | Task 3 runs `pnpm dlx @axe-core/cli http://$PROD_URL` against the deployed gradient. If any color-contrast violation surfaces, escalate to the executor for the appropriate accent-token tune (RESEARCH §"UI-SPEC §4.2" pre-computed ratios suggest `#7CFF6B` is safe but real-orb-position validation is the actual proof). |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Per-moment reduced-motion compliance across all 7 UI-SPEC §6.5 motion moments</name>
  <files>app/globals.css, components/extract/LoadingStages.tsx, components/workout/SkeletonCard.tsx, components/workout/ExerciseCard.tsx, components/layout/AmbientBackground.tsx</files>
  <read_first>
    - app/globals.css (Plan 1's global `@media (prefers-reduced-motion: reduce)` block — already covers `.orb-{1,2,3}`, `.skeleton-shimmer`, `.pulse-dot`, `.card-hover-lift`; Plan 4 audits + refines)
    - components/extract/LoadingStages.tsx (Motion `AnimatePresence` dot→checkmark — already respects useReducedMotion in Plan 1; verify)
    - components/workout/SkeletonCard.tsx (Plan 1's shimmer keyframe — verify the class name matches the `@media` selector)
    - components/workout/ExerciseCard.tsx (Plan 1's form-cue expansion — Motion `height` animation + chevron rotate; verify gated by `useReducedMotion()`)
    - components/workout/WorkoutView.tsx (Plan 1's cascade — already gated; verify)
    - components/layout/AmbientBackground.tsx (Plan 1's orb container — verify `.orb-1/2/3` class names match globals.css)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §6.5 (exhaustive list of 7 moments)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Common Pitfalls / Pitfall 7" (the implementation-gap warning)
  </read_first>
  <action>
**Audit pass — enumerate each of the 7 motion moments and verify both code-paths handle reduced motion.**

For each of the 7 moments in UI-SPEC §6.5, do the following audit + fix:

**Moment 1 — Gradient orb drift** (CSS keyframes `@keyframes drift-orb-1`, `drift-orb-2`, `drift-orb-3` in `app/globals.css`):
- Inspect `app/globals.css` for the keyframes. Each orb should have `.orb-N { animation: drift-orb-N {28s|36s|44s} ease-in-out infinite alternate; }`.
- Confirm the existing `@media (prefers-reduced-motion: reduce) { .orb-1, .orb-2, .orb-3 { animation: none !important; } }` block from Plan 1 is present.
- Verify `components/layout/AmbientBackground.tsx` actually applies the `orb-1`, `orb-2`, `orb-3` classes (not e.g., `orb-primary`). If class names mismatch, fix the className strings to align with the CSS selectors.

**Moment 2 — Active-stage pulse-dot scale**:
- Inspect `app/globals.css` for `.pulse-dot { animation: pulse-dot 1.4s ease-in-out infinite; }` keyframes.
- Confirm the `@media (prefers-reduced-motion: reduce) { .pulse-dot { animation: none !important; } }` rule exists.
- Verify `components/extract/LoadingStages.tsx` applies `className="pulse-dot"` (or equivalent) to the active stage's dot element. If the dot uses Motion's `<motion.div animate={{ scale: [1, 1.25, 1] }}>` instead of CSS, gate it via `useReducedMotion()` — if `reduceMotion === true`, render a static dot with no `animate` prop.

**Moment 3 — Skeleton shimmer sweep**:
- Inspect `app/globals.css` for the shimmer keyframes (`@keyframes shimmer-sweep`) and the `.skeleton-shimmer` class declaration.
- Confirm the reduced-motion block has `.skeleton-shimmer { animation: none !important; }`.
- Verify `components/workout/SkeletonCard.tsx` applies `className="skeleton-shimmer"` (or equivalent) to the shimmer overlay pseudo-element or inner div.

**Moment 4 — Result-cascade stagger** (Motion, already Plan 1):
- Open `components/workout/WorkoutView.tsx` and confirm: `const reduceMotion = useReducedMotion();` is at the top of the component, AND when `reduceMotion === true`, `animate` and `initial` are both set to `false` (effectively disabling Motion).
- Also confirm: when `shouldAnimateIn === false` (share-link hydration), the same disabled-Motion path is taken. The combined check is `const shouldAnimate = shouldAnimateIn && !reduceMotion;`.

**Moment 5 — Card hover-lift** (CSS):
- Inspect `app/globals.css` for the `.glass-card:hover { transform: translateY(-2px); ... }` rule (or equivalent). Add the rule if not present per UI-SPEC §5.3 — `transform: translateY(-2px)`, accumulated `box-shadow` with 6px `--color-accent-glow` bottom-shadow, `border-bottom-color: rgba(var(--color-accent-rgb), 0.4)`, `transition: 200ms cubic-bezier(0.22, 1, 0.36, 1)`.
- Add the `(hover: hover) and (pointer: fine)` media query around the `:hover` rule so it only fires on desktop (UI-SPEC §5.3 verbatim).
- Confirm the reduced-motion block contains `.card-hover-lift, .glass-card:hover { transition: none !important; transform: none !important; }`.
- Verify `components/workout/ExerciseCard.tsx` and `components/workout/SupersetCard.tsx` apply `className="glass-card"` (already there from Plan 1) — the hover rule fires on the class globally.

**Moment 6 — Form-cue expansion** (Motion, already Plan 1):
- Open `components/workout/ExerciseCard.tsx` and confirm the form-cue toggle uses `useReducedMotion()` to gate both the chevron rotation and the height animation:
  - When `reduceMotion === true`: the toggle is instant (`animate={false}` or skip the Motion wrapper entirely, render an unanimated conditional).
  - When `reduceMotion === false`: use the Motion `height: isOpen ? "auto" : 0` animation with cubic-bezier(0.22, 1, 0.36, 1) over 200ms.
- Add the missing case if not already present.

**Moment 7 — Toast entrance** (sonner):
- Open `app/layout.tsx` and inspect the `<Toaster>` props. Sonner respects `prefers-reduced-motion` by default since v1.x — the entrance animation collapses to a fade-only when reduced. Verify by toggling Reduce Motion and confirming the toast does not slide-in.
- If sonner's default doesn't suffice (e.g., observation shows the slide-in still occurs), explicitly add a `<style>` block in `globals.css` overriding the sonner classes:
  ```css
  @media (prefers-reduced-motion: reduce) {
    [data-sonner-toast] { animation: none !important; transition: opacity 100ms !important; }
  }
  ```

Add a "Reduced Motion audit comment" at the top of `app/globals.css` enumerating all 7 moments and their gating mechanisms — so future contributors don't have to re-discover the list. Format:
```
/*
 * Reduced Motion Audit (UI-SPEC §6.5):
 * 1. Orb drift — CSS @media block below
 * 2. Pulse-dot scale — CSS @media block below
 * 3. Skeleton shimmer — CSS @media block below
 * 4. Result cascade — Motion useReducedMotion() in WorkoutView.tsx
 * 5. Card hover-lift — CSS @media block below + (hover: hover) media query
 * 6. Form-cue expansion — Motion useReducedMotion() in ExerciseCard.tsx
 * 7. Toast entrance — sonner default + CSS @media override below
 */
```

After all 7 moments are audited, run `pnpm dev` and toggle macOS System Settings → Accessibility → Display → Reduce Motion. Reload the page and walk through every state (idle → submit → loading → success → click Share → see toast → hover a card on desktop → toggle a form-cue expand). Confirm: NO motion in ANY of the 7 moments — only color/state changes (which UI-SPEC §6.5 preserves explicitly).
  </action>
  <verify>
    <automated>grep -A 20 "@media (prefers-reduced-motion: reduce)" app/globals.css | grep -cE "\.orb-[123]|\.pulse-dot|\.skeleton-shimmer|\.card-hover-lift|\.glass-card:hover|data-sonner-toast" | awk '$1 >= 5 { exit 0 } { exit 1 }' && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains the "Reduced Motion Audit" comment listing all 7 moments. Verify: `grep -c "Reduced Motion Audit" app/globals.css` ≥ 1.
    - `app/globals.css` `@media (prefers-reduced-motion: reduce)` block contains ALL of: `.orb-1`, `.orb-2`, `.orb-3`, `.pulse-dot`, `.skeleton-shimmer`, `.card-hover-lift` (or `.glass-card:hover`), and a `[data-sonner-toast]` override (if sonner's default isn't sufficient). Verify: `grep -A 30 "@media (prefers-reduced-motion: reduce)" app/globals.css | grep -cE "orb-1|orb-2|orb-3|pulse-dot|skeleton-shimmer|card-hover-lift|glass-card:hover|data-sonner-toast"` ≥ 5.
    - `components/workout/WorkoutView.tsx` uses `useReducedMotion()` to gate animate/initial. Verify: `grep -cE "useReducedMotion\(\)|shouldAnimate.*reduceMotion" components/workout/WorkoutView.tsx` ≥ 2.
    - `components/workout/ExerciseCard.tsx` uses `useReducedMotion()` to gate form-cue expansion. Verify: `grep -c "useReducedMotion" components/workout/ExerciseCard.tsx` ≥ 1.
    - `components/extract/LoadingStages.tsx` either uses `useReducedMotion()` for the pulse-dot OR applies `className="pulse-dot"` to a CSS-keyframed element. Verify by reading the source — either path is acceptable; document choice in SUMMARY.
    - `components/layout/AmbientBackground.tsx` applies classNames `orb-1`, `orb-2`, `orb-3` matching globals.css selectors. Verify: `grep -cE "orb-[123]" components/layout/AmbientBackground.tsx` ≥ 3.
    - `components/workout/SkeletonCard.tsx` applies a class matching the `.skeleton-shimmer` selector. Verify: `grep -c "skeleton-shimmer" components/workout/SkeletonCard.tsx` ≥ 1.
    - `app/globals.css` includes the `@media (hover: hover) and (pointer: fine)` guard for `.glass-card:hover`. Verify: `grep -c "(hover: hover)" app/globals.css` ≥ 1.
    - `pnpm typecheck && pnpm build` exits 0.
    - Manual smoke (executor performs locally with `pnpm dev`): toggle macOS Reduce Motion ON, reload, walk through every UI state, confirm no orb drift, no pulse, no shimmer, no cascade, no hover-lift, no chevron rotate, no slide-in toast.
  </acceptance_criteria>
  <done>All 7 motion moments enumerated in UI-SPEC §6.5 are systematically gated by either CSS `@media (prefers-reduced-motion: reduce)` or Motion's `useReducedMotion()` hook. The Reduced Motion audit comment in globals.css documents the gating mechanism for each moment. Manual toggle test confirms zero motion under reduced-motion preference.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mobile sticky-bottom ActionBar + tooltip disambiguation on Copy buttons + 44px touch-target audit + mobile glass perf branch</name>
  <files>components/workout/ActionBar.tsx, components/workout/ExerciseCard.tsx, app/globals.css, components/extract/ErrorState.tsx</files>
  <read_first>
    - components/workout/ActionBar.tsx (Plan 1's inline desktop variant — to extend with mobile sticky-bottom)
    - components/workout/ExerciseCard.tsx (Plan 1's form-cue toggle — confirm 44px touch target on the button)
    - components/extract/ErrorState.tsx (Plan 2's recovery buttons — confirm 44px height)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §9.4 Mobile (sticky-bottom action bar) + §9.5 Touch targets + §8.1 (tooltip on Copy buttons) + §5.2 (mobile glass fallback recipe)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-CONTEXT.md `<deferred>` (mobile-glass perf fallback acknowledgment)
    - components/ui/tooltip.tsx (the shadcn primitive installed in Plan 1)
  </read_first>
  <action>
**Mobile sticky-bottom ActionBar.** Update `components/workout/ActionBar.tsx` className to use Tailwind responsive utilities per `<interfaces>` block. The pattern:
```tsx
<div className="fixed bottom-0 left-0 right-0 z-30 bg-[var(--color-surface-glass)] backdrop-blur-xl border-t border-white/10 pb-[env(safe-area-inset-bottom)] px-6 py-3 flex gap-3 justify-around md:relative md:bottom-auto md:left-auto md:right-auto md:px-0 md:py-0 md:bg-transparent md:backdrop-blur-none md:border-0 md:pb-0 md:justify-end md:gap-3 md:mt-6">
  {/* 4 buttons */}
</div>
```
Below `md` breakpoint (mobile): fixed-bottom glass bar with safe-area padding and centered/spaced buttons. At `md`+: relative inline with right-aligned buttons + 24px top margin.

Add bottom-padding to the WorkoutView container on mobile so content doesn't get clipped by the sticky bar — `pb-24 md:pb-0` on the `<motion.article>` container in `WorkoutView.tsx`.

Each button retains its `h-11` (44px) height (Plan 1 set this). Verify by reading the source. On mobile, 4 buttons may overflow horizontally — set the buttons to icon-only on the smallest widths via Tailwind `[&_span.label-text]:hidden md:inline` or by switching to text+icon on mobile with smaller spacing. Cleanest: convert the labels to short forms on mobile (`Copy MD` / `Copy Plain` / `Share`) using `<span className="md:hidden">Copy MD</span><span className="hidden md:inline">Copy as Markdown</span>`.

**Tooltip disambiguation on Copy buttons.** Wrap the two Copy buttons (Copy as Markdown + Copy as Plain Text) in `<Tooltip><TooltipTrigger asChild>...</TooltipTrigger><TooltipContent className="hidden md:block">{description}</TooltipContent></Tooltip>` per `<interfaces>` block pattern. Imports come from `@/components/ui/tooltip` (installed in Plan 1). Use `<TooltipProvider>` wrapping the ActionBar (or higher up in `layout.tsx` once globally). Content:
- Copy as Markdown → tooltip text: "Copy a markdown representation of this workout"
- Copy as Plain Text → tooltip text: "Copy a plain-text version (no markdown syntax) for Notes / WhatsApp"
The `hidden md:block` class on `<TooltipContent>` hides tooltips below 768px (mobile doesn't have hover anyway).

**44px touch-target audit.** Walk the full Plan 1+2+3 component tree and verify every interactive element on mobile is ≥ 44px:
- Extract Workout CTA — `h-14` (56px) ✓ from Plan 1
- ActionBar buttons — `h-11` (44px) ✓ from Plan 1; verify in this plan
- Form-cue expand toggle in ExerciseCard — currently rendered as a `<button>` with text; ensure `min-h-[44px]` or `py-3` to hit 44px. If the toggle is a small inline button, refactor to a full-width clickable row.
- ErrorState recovery button — `size="lg"` or `h-11` ✓ from Plan 2; verify
- URL input height — likely `h-11` via shadcn defaults; verify on the shadcn `<Input>` className overrides
- Any other interactive — form labels, copy-link button if separate, etc.

For each, add an inline source comment `/* 44px touch target — OUTV-07 */` and a Tailwind `min-h-[44px]` or `h-11` class to make the requirement explicit. If any element is currently under 44px, fix it.

**Mobile glass perf fallback.** Append to `app/globals.css` the additional `@media` branch from `<interfaces>`:
```css
/* Plan 4 — mobile glass perf fallback heuristic (UI-SPEC §5.2; full validation deferred per CONTEXT.md <deferred>) */
@media (prefers-reduced-motion: reduce) and (max-width: 480px) {
  .glass-card {
    background-color: rgba(20, 22, 28, 0.85);
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }
}
```
This is a heuristic — actual mid-range Android performance validation is deferred (CONTEXT.md `<deferred>` + STATE.md "Blockers/Concerns"). Document this disposition in the Plan 4 SUMMARY.

Run `pnpm dev`. On a real mobile device or Chrome DevTools mobile emulator (iPhone 12 / iPhone 14 / Pixel 5 viewports), confirm:
- Sticky-bottom ActionBar appears glass-styled at the viewport bottom with proper safe-area padding.
- Scrolling the exercise list does NOT scroll the action bar — it stays fixed.
- Tapping each button (Extract / Copy MD / Copy Plain / Share / Watch / form-cue toggle / ErrorState recover) shows a satisfying tap target — no near-misses.
- Switch to desktop viewport — action bar reverts to inline right-aligned. Tooltips appear on hover.
- macOS Reduce Motion + <480px width: glass cards drop the backdrop-filter (verify via DevTools Computed pane → look for `backdrop-filter: none`).
  </action>
  <verify>
    <automated>grep -c "fixed bottom-0\|pb-\[env(safe-area-inset-bottom)\]" components/workout/ActionBar.tsx | grep -E "^[12]$" && grep -c "TooltipTrigger\|TooltipContent" components/workout/ActionBar.tsx | grep -E "^[2-9]$|^[1-9][0-9]+$" && grep -c "(prefers-reduced-motion: reduce) and (max-width: 480px)" app/globals.css | grep -E "^[1-9]" && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `components/workout/ActionBar.tsx` contains `fixed bottom-0` AND `pb-[env(safe-area-inset-bottom)]` AND `md:relative` (responsive switch). Verify: `grep -cE "fixed bottom-0|pb-\[env\(safe-area-inset-bottom\)\]|md:relative" components/workout/ActionBar.tsx` ≥ 3.
    - `components/workout/ActionBar.tsx` wraps Copy as Markdown and Copy as Plain Text buttons in `<Tooltip>`. Verify: `grep -c "TooltipTrigger" components/workout/ActionBar.tsx` ≥ 2 (one per Copy button).
    - Tooltip content text is present and distinguishing. Verify: `grep -c "markdown representation\|plain-text version" components/workout/ActionBar.tsx` ≥ 2.
    - Tooltip is hidden on mobile via responsive class. Verify: `grep -c 'hidden md:block\|md:inline-block' components/workout/ActionBar.tsx` ≥ 1.
    - `app/globals.css` includes the `(prefers-reduced-motion: reduce) and (max-width: 480px)` glass fallback. Verify: `grep -c "(prefers-reduced-motion: reduce) and (max-width: 480px)" app/globals.css` ≥ 1.
    - 44px touch-target compliance (sample sources):
      - `grep -cE 'h-11|h-14|min-h-\[44px\]|size="lg"' components/workout/ActionBar.tsx` ≥ 1
      - `grep -cE 'h-11|h-14|min-h-\[44px\]' components/extract/ErrorState.tsx` ≥ 1
      - `grep -cE 'min-h-\[44px\]|h-11|py-3' components/workout/ExerciseCard.tsx` ≥ 1
    - `components/workout/WorkoutView.tsx` adds bottom padding for mobile sticky bar. Verify: `grep -cE 'pb-24|pb-20' components/workout/WorkoutView.tsx` ≥ 1.
    - `pnpm typecheck && pnpm build` exits 0.
    - Manual smoke (executor confirms via Chrome DevTools mobile emulator on `pnpm dev`):
      - At <768px width, ActionBar is fixed-bottom and glass-styled.
      - At ≥768px width, ActionBar is inline right-aligned.
      - Hover over Copy as Markdown on desktop shows tooltip; mobile shows none.
      - All buttons are tap-friendly (no missed taps in manual test).
  </acceptance_criteria>
  <done>The ActionBar is responsive: fixed-bottom glass on mobile with safe-area padding, inline right-aligned on desktop. Tooltips disambiguate the two Copy buttons on desktop. Every interactive element on mobile is ≥ 44px. Mobile glass perf fallback heuristic is in place pending real-device validation (deferred per CONTEXT.md).</done>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 3: Deploy + axe-core WCAG audit against production gradient + cross-device share-link smoke test (Phase 1 exit gate)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (to update phase status to complete)
    - .planning/ROADMAP.md (to mark Phase 1 box checked)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-RESEARCH.md §"Security Domain / Phase 1 security gate" (the deploy-time security checks)
    - .planning/phases/01-mock-deployable-premium-ui-demo/01-UI-SPEC.md §4.2 (pre-computed contrast ratios) + §14 Accessibility Contract
    - Vercel project dashboard for $PROD_URL (production URL from Plan 1 Task 5)
  </read_first>
  <action>
Commit all Plan 4 changes via conventional commit `feat(01): polish — reduced-motion all 7 moments, mobile sticky ActionBar, tooltips, axe-core audit setup`. Push to `main`. Wait for Vercel auto-deploy to complete.

Capture the production URL (`$PROD_URL`) — same URL from Plan 1 Task 5 (or whatever the user provided then). If a new preview URL is generated by Vercel, capture it.

Run the production WCAG audit via the executor's Bash tool:
```bash
pnpm dlx @axe-core/cli "$PROD_URL" --tags wcag2a,wcag2aa --save tmp/axe-report.json
```
Inspect the output for `color-contrast` violations specifically (the DSGN-06 closing check). If any violations surface:
- Identify which element + which orb-position is causing the failure.
- If the accent green `#7CFF6B` fails against any glass-over-orb region, walk the green ramp slightly cooler (e.g., `#7BFF7B` or `#80FF80`) and re-test. RESEARCH §"UI-SPEC §4.2" pre-computed 11.8:1 ratio over the darkest region — failures suggest the lightest orb region (coral or green's own orb) is pushing effective backdrop luminance up.
- If body-text `#F4F6F8` fails on the violet-orb region, increase opacity of `--color-surface-glass` from 0.11 to 0.13 (still within CONTEXT.md D-03 10–12% range).
- Re-run axe-core after each fix; commit and redeploy.

If axe-core reports zero violations on the production URL, proceed.

**Phase 1 cross-device share-link smoke test (the ROADMAP-success-criterion-#3 + OPS-02 acceptance gate):**

This is a HUMAN CHECKPOINT GATE.

Ask the user to perform these steps and confirm each:
1. Open `$PROD_URL` in browser A on device A (e.g., MacBook Chrome).
2. Paste any YouTube URL (e.g., `https://www.youtube.com/watch?v=dQw4w9WgXcQ`). Wait for the workout to render.
3. Click "Share Workout". The toast appears confirming "Share link copied".
4. Take the copied URL out-of-band — e.g., paste into Slack, AirDrop to phone, or message yourself.
5. Open the share URL in browser B on device B (e.g., iPhone Safari, or Firefox on another machine, or an incognito window on Edge — anything that's not browser A's session).
6. Confirm: the same workout renders identically with no SSE stream, no cascade animation, no backend call. The strip notice appears if the fixture happened to trigger D-17 stripping.
7. On browser B, click Extract Workout, paste `https://www.youtube.com/watch?v=fail-demo-1`. After ~500ms, confirm the NETWORK ErrorState appears.
8. Toggle device B's "Reduce Motion" accessibility setting (iOS: Settings → Accessibility → Motion → Reduce Motion; Android: Settings → Accessibility → Remove animations). Reload `$PROD_URL`. Confirm: orbs are static, paste a URL → loading view has static dots and no shimmer, result renders with simultaneous cross-fade (no stagger), action bar shows no toast slide.

Resume signal: user replies "phase 1 verified — production at $PROD_URL". Optionally, the user shares a screenshot of the workout-on-second-device for the SUMMARY artifact.

**Finalize STATE.md and ROADMAP.md:**

Update `.planning/STATE.md`:
- Set `progress.total_plans: 4`, `progress.completed_plans: 4`, `progress.percent: 33` (Phase 1 of 3 done).
- Set `progress.completed_phases: 1`.
- Update `Current Position`: `Phase: 1 of 3 (COMPLETE — Mock-Deployable Premium UI Demo)`, `Plan: 4 of 4`, `Status: Phase 1 shipped to $PROD_URL`.
- Update progress bar: `[███░░░░░░░] 33%`.
- Add to "Accumulated Context / Decisions": `Phase 1 shipped to $PROD_URL on {ISO date} — 4 plans, ~{N} commits. All 36 Phase 1 REQ-IDs traceable to completed plans.`
- Update `stopped_at` and `last_activity`.

Update `.planning/ROADMAP.md`:
- Change Phase 1's leading `- [ ]` to `- [x]`.
- Under `## Progress` table, change Phase 1 row to `1/4 plans complete` → wait actually it's 4/4 plans complete; update accordingly: `**Phase 1** | 4/4 | Complete | {date}`.

Commit the state finalization with `chore(state): mark Phase 1 complete — shipped to $PROD_URL`.
  </action>
  <verify>
    <automated>echo "Axe-core report inspection is human-checkpoint dependent — automated verification limited to commit existence + state file finalization" && grep -c "Phase 1" .planning/ROADMAP.md && grep -E "completed_phases: 1|completed_plans: 4" .planning/STATE.md | grep -c "completed_phases: 1\|completed_plans: 4"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm dlx @axe-core/cli "$PROD_URL" --tags wcag2a,wcag2aa` reports zero `color-contrast` violations against the deployed Vercel URL.
    - `.planning/STATE.md` updated: `completed_phases: 1`, `completed_plans: 4`, `percent: 33`, `stopped_at` and `last_activity` reference Plan 4 ship + `$PROD_URL`.
    - `.planning/ROADMAP.md` Phase 1 entry has `- [x]` and the progress table shows `4/4 plans complete`.
    - Human checkpoint: user confirms steps 1–8 of the cross-device smoke test pass on the production URL.
    - `git log --oneline -3` shows the Plan 4 commits including the state finalization.
    - The 5 ROADMAP success criteria for Phase 1 are all verifiably true on `$PROD_URL`:
      1. Live Vercel URL exists with paste → fixture-render in ~3s (actually ~4–5s per D-07, which honestly meets the spec): YES ($PROD_URL).
      2. Loading streams real SSE stage events with skeleton cards; EXTRACT_MODE swaps mock→real with no frontend change: YES (curl confirms SSE; `lib/extraction/service.ts` factory + `RealExtractionService` stub for swap point).
      3. Copy as Markdown, Copy as Plain Text, Share Workout (recreates view on open with no backend round-trip): YES.
      4. Invalid URLs rejected inline before any API call; extraction failures and no-workout detected land on distinct error states with retry: YES.
      5. Zod `Workout` schema in `lib/schema/workout.ts` is single source of truth, includes forward-looking fields, byte-compatible with brief: YES.
  </acceptance_criteria>
  <done>The deployed Vercel URL passes axe-core WCAG 2AA color-contrast checks on the live gradient; the user confirms cross-device + reduced-motion share-link round-trip works in production; STATE.md and ROADMAP.md are finalized; Phase 1 is shipped.</done>
</task>

</tasks>

<verification>
  After Task 3 the following must all hold:
  - `pnpm test && pnpm typecheck && pnpm build` exits 0 across all four commands (all prior plans' tests still green).
  - axe-core reports zero color-contrast violations on `$PROD_URL`.
  - Manual macOS Reduce Motion toggle on `$PROD_URL` produces static UI across all 7 motion moments.
  - Cross-device share-link round-trip works (user-confirmed checkpoint).
  - All 36 Phase 1 requirement IDs are addressed by one or more of Plans 1–4:
    - Plan 1: INPT-01, INPT-02 (basic), INPT-03, SCHM-01..05, PIPE-01..06 (minus PIPE-05 explicitly to Plan 2 expansion), OUTV-01, OUTV-03, OUTV-04, OUTV-05, OUTV-06, SHRE-01, SHRE-02, SHRE-03 (basic), DSGN-01..04, OPS-01, OPS-02, OPS-03
    - Plan 2: INPT-02 (full), PIPE-05 (skeletons across error states too), OUTV-02 (difficulty across 3 values), OUTV-07 (touch on error CTAs), ERRS-01..03
    - Plan 3: SHRE-03 (full with strip chain + schema versioning)
    - Plan 4: DSGN-05 (per-moment reduced-motion), DSGN-06 (axe-core on prod gradient), OUTV-07 (full mobile audit + sticky bottom)
  - `.planning/STATE.md` records `completed_phases: 1`.
  - `.planning/ROADMAP.md` Phase 1 box is `[x]`.
</verification>

<success_criteria>
  Phase 1 is complete. The deployed Vercel URL `$PROD_URL` meets ALL FIVE ROADMAP Phase 1 success criteria verbatim:
  1. ✓ Live Vercel URL where a user can paste any YouTube URL and see a fully-rendered fixture workout in premium dark glassmorphism within ~3 seconds (actually ~4–5s per D-07 — slower-than-brief is correct).
  2. ✓ Loading streams real SSE stage events from `/api/extract`; `EXTRACT_MODE` env var would swap mock → real with no frontend change.
  3. ✓ User can Copy as Markdown, Copy as Plain Text, and generate a Share Workout link recreating the view with no backend round-trip.
  4. ✓ Invalid URLs rejected inline; extraction failures + no-workout-detected results land on distinct honest error states with retry.
  5. ✓ Zod `Workout` schema in `lib/schema/workout.ts` is single source of truth with forward-looking fields (startTimestamp, sourceQuote, equipment[], extraction_confidence, schema_version), byte-compatible with brief's example.

  Phase 1 ships. Phase 2 begins by adding `lib/extraction/real.ts` with the real captions + GPT-4o `generateObject(WorkoutSchema)` pipeline and flipping `EXTRACT_MODE=real` in production — no frontend change required, no schema change required, no UI rework required. The architectural decisions in `01-SKELETON.md` carry forward.
</success_criteria>

<output>
Create `.planning/phases/01-mock-deployable-premium-ui-demo/01-04-SUMMARY.md` capturing:
- The final production URL `$PROD_URL`.
- axe-core output summary (zero violations, or list of remediated violations if any).
- The chosen Plan 4 mobile-CSS choices (any accent-green retune, any opacity adjustment of `--color-surface-glass`).
- The cross-device share-link smoke test confirmation from the user (with screenshot reference if provided).
- A "Phase 2 hand-off" note: the swap point is `lib/extraction/real.ts` (Plan 1 stub with explicit throw); the env var is `EXTRACT_MODE=real`; the frontend requires zero changes; the cost-protection stack (rate limit, cache, daily cap, OpenAI + Vercel budget caps, DMCA page, daily smoke test) MUST ship in the same release as the first real OpenAI key per Phase 2 ROADMAP scope.
- An honest acknowledgment of any deferred items NOT closed in Phase 1: real-device mid-range Android glass-perf validation (CONTEXT.md `<deferred>`); SSR pre-decode for `?w=` to avoid input-flash (RESEARCH §"Pitfall 2"); useTransition for SSE dispatch on low-end devices (RESEARCH §"Open Question 5"). Each should be confirmed as Phase 4 / future polish, not Phase 1 gaps.
</output>
