---
phase: 02-real-captions-pipeline-cost-protections
plan: 07
type: execute
wave: 6
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - COST-04
  - EXTR-04

must_haves:
  truths:
    - All 8 cost defenses are verifiable in the codebase before EXTRACT_MODE=real is set in Vercel Production
    - OpenAI dashboard has a monthly budget alert configured (D-20c, COST-04)
    - Vercel Production environment has EXTRACT_MODE=real set (the "flip")
    - Vercel Production deploy with EXTRACT_MODE=real is live at https://exercised-ten.vercel.app
    - A manual smoke test against the production URL with a real captioned video returns a real extraction
    - STATE.md updated with "Phase 2 shipped YYYY-MM-DD; all 8 cost defenses verified; first real OpenAI key deployed"
  artifacts:
    - path: .planning/STATE.md
      provides: Updated project state with Phase 2 completion
      contains: "EXTRACT_MODE=real"
    - path: .planning/PROJECT.md
      provides: Key Decisions table updated with Phase 2 completion
      contains: "first real OpenAI key deployed"
  key_links:
    - from: Vercel Production
      to: EXTRACT_MODE=real env var
      via: Vercel dashboard → Project → Environment Variables → Production
      pattern: "EXTRACT_MODE"
---

<objective>
The final ship gate. This plan runs a pre-flight audit of all 8 cost defenses, executes the owner-action for OpenAI and Vercel dashboard budget caps (the only thing Claude cannot automate), flips EXTRACT_MODE=real in Vercel Production, and verifies the live production deployment returns a real extraction.

Purpose: The critical non-negotiable from PROJECT.md — all 8 cost defenses MUST ship in the same release as the first real OpenAI key. This plan is structured so that it is literally impossible to flip the switch without verifying each defense is present. The defense audit is automated (grep/cat assertions); the owner actions are checkpointed.

Output: Production deployment with EXTRACT_MODE=real live, all 8 defenses verified, STATE.md + PROJECT.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-real-captions-pipeline-cost-protections/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight audit — verify all 8 cost defenses are in the codebase</name>
  <files>
    (read-only audit — no files modified)
  </files>
  <read_first>
    app/api/extract/route.ts,
    lib/ratelimit/index.ts,
    lib/spend/cap.ts,
    lib/cache/videoCache.ts,
    lib/guards/sourceQuote.ts,
    components/extract/ConfidenceBanner.tsx,
    app/about/page.tsx,
    app/api/cron/smoke/route.ts,
    vercel.json
  </read_first>
  <action>
    Run the following assertions using Bash commands. Each assertion must return a truthy/nonzero result. If any assertion fails, STOP and report which defense is missing — do not proceed to Task 2.

    Defense 1 (COST-02 — per-IP rate limit):
    grep -c "checkRateLimit" app/api/extract/route.ts
    grep -c "slidingWindow(5" lib/ratelimit/index.ts
    grep -c "slidingWindow(20" lib/ratelimit/index.ts

    Defense 2 (COST-03 — global daily spend cap):
    grep -c "checkSpendCap" app/api/extract/route.ts
    grep -c "incrementSpend" app/api/extract/route.ts
    grep -c "redis.incr" lib/spend/cap.ts

    Defense 3 (COST-01 — videoId cache):
    grep -c "getCached" app/api/extract/route.ts
    grep -c "setCached" app/api/extract/route.ts
    grep -c "extract:v1:" lib/cache/videoCache.ts

    Defense 4 (EXTR-03 — sourceQuote hallucination guard):
    grep -c "validateSourceQuotes" lib/extraction/real.ts
    grep -c "droppedCount" lib/guards/sourceQuote.ts

    Defense 5 (ERRS-04 — low-confidence banner):
    grep -c "lowConfidence" components/extract/ConfidenceBanner.tsx
    grep -c 'role="status"' components/extract/ConfidenceBanner.tsx

    Defense 6 (OPS-04 — DMCA /about page):
    grep -c "Exercised extracts workout routines" app/about/page.tsx
    grep -c "hello@exercised.app" app/about/page.tsx
    grep -c '"/about"' components/layout/Footer.tsx

    Defense 7 (OPS-05 — daily smoke test):
    grep -c "CRON_SECRET" app/api/cron/smoke/route.ts
    grep -c '"path": "/api/cron/smoke"' vercel.json

    Defense 8 (COST-04 — OpenAI/Vercel dashboard caps):
    This defense cannot be automated — verified in Task 2 (human checkpoint).

    Also verify no .optional() snuck back into the schema:
    grep -c "\.optional()" lib/schema/workout.ts — MUST return 0

    Also run: pnpm test --run (must exit 0)
    Also run: pnpm build (must exit 0)

    Report each assertion result. If all pass (and pnpm test + pnpm build exit 0), confirm "All 7 automatable defenses verified. Proceeding to Task 2 for owner actions."
  </action>
  <verify>
    <automated>pnpm test --run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - All 7 grep assertions return >= 1 (except the .optional() check which must return 0)
    - pnpm test --run exits 0
    - pnpm build exits 0
    - lib/schema/workout.ts contains zero .optional() fields
  </acceptance_criteria>
  <done>All 7 automatable cost defenses verified present in codebase. Build and tests clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner action — configure OpenAI dashboard budget alert + Vercel env vars</name>
  <what-built>Claude has verified all 7 automatable cost defenses. The 8th defense (D-20c, COST-04) requires owner action in two external dashboards.</what-built>
  <how-to-verify>
    Complete these steps in order:

    STEP 1 — OpenAI dashboard budget alert:
    1. Go to https://platform.openai.com/settings/organization/limits
    2. Set a monthly budget alert at $10 (or your preferred threshold)
    3. Confirm the alert email will go to your registered OpenAI account email
    Note: as of 2026, OpenAI monthly budget limits are alerts only (not hard stops). The application-level Redis spend cap (D-20c, $5/day) is the hard enforcement mechanism. The OpenAI alert is a secondary warning.

    STEP 2 — Vercel environment variables:
    1. Go to https://vercel.com/dashboard → your Exercised project → Settings → Environment Variables
    2. Add or confirm all required env vars are set for Production:
       - UPSTASH_REDIS_REST_URL (from Upstash console)
       - UPSTASH_REDIS_REST_TOKEN (from Upstash console)
       - OPENAI_API_KEY (from OpenAI dashboard)
       - DAILY_EXTRACTION_CAP=250
       - CRON_SECRET (random hex string)
       - RESEND_API_KEY (from Resend dashboard)
       - GITHUB_TOKEN (GitHub PAT with repo scope)
       - GITHUB_REPO (e.g. username/exercised)
       - ALERT_EMAIL (reachable email — BLOCKER from D-24d)
    3. Confirm EXTRACT_MODE is NOT yet set to "real" in Production (still "mock" or not set)

    STEP 3 — Vercel Spend Management (Hobby plan note):
    On Hobby plan, Vercel Spend Management auto-pause is not available. The application-level Redis cap is the enforcement mechanism. If you are on Hobby, note this and continue.
    If you are on Pro: go to https://vercel.com/dashboard → Settings → Billing → Spend Management and configure a monthly budget alert.
  </how-to-verify>
  <resume-signal>Type "owner-actions-complete" when OpenAI budget alert is configured and all Vercel Production env vars are set. Note the status of ALERT_EMAIL blocker. If still pending, type "email-pending" and we will configure a personal email as a temporary ALERT_EMAIL.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Flip the switch — set EXTRACT_MODE=real in Vercel Production</name>
  <what-built>All 8 cost defenses verified. All env vars set. The only remaining action is flipping EXTRACT_MODE=real.</what-built>
  <how-to-verify>
    1. In Vercel dashboard: Project → Settings → Environment Variables → Production
    2. Add EXTRACT_MODE=real (or update from "mock" to "real")
    3. Trigger a new deployment: go to Deployments tab → Redeploy latest
    4. Wait for deployment to complete (usually 60-90 seconds)
    5. Once deployed, test the production URL:
       - Go to https://exercised-ten.vercel.app
       - Paste a real captioned YouTube workout video URL (use one from your eval set)
       - Watch the pipeline: "Fetching captions..." → "Transcribing..." → "Analyzing..." → "Generating..."
       - Confirm the result is a real workout (not a fixture) — title should match the actual video
       - Confirm the "Watch on YouTube" link goes to the video URL (not just the channel)

    6. Optional: test a non-fitness video URL — confirm it returns "No workout in this video."
    7. Optional: test that rate limiting works — make 6 requests from the same IP in quick succession (the 6th should return the RATE_LIMITED error)

    If the production extraction fails with a NETWORK error on the first real video:
    - Check Vercel function logs for the actual error
    - Common cause: YouTube IP blocking for Vercel's serverless IPs (RESEARCH Pitfall 1)
    - If this happens consistently, the caption libraries are being blocked — document the finding and consider acceleration to Phase 3 (Supadata or Railway sidecar)
  </how-to-verify>
  <resume-signal>Type "production-live" if the real extraction works on the production URL. Type "production-blocked: [describe the error]" if the extraction fails consistently — this triggers a post-mortem and potential plan adjustment.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md + PROJECT.md + REQUIREMENTS.md</name>
  <files>.planning/STATE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/STATE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    Update .planning/STATE.md:
    - Update status to "Phase 2 shipped to https://exercised-ten.vercel.app on YYYY-MM-DD (use today's date)"
    - Update stopped_at to "Phase 3 planning"
    - Update progress: completed_phases: 2, total_plans: 12, completed_plans: 12, percent: 67
    - Update Current Position section: Phase 2 of 3 (COMPLETE) → ready to start Phase 3
    - Add to Decisions section: "Phase 2 shipped: first real OpenAI key deployed YYYY-MM-DD; all 8 cost defenses verified; EXTRACT_MODE=real live in Vercel Production"
    - Update Blockers/Concerns: remove "Phase 2 prep" items; add "Phase 3 prep: Supadata vs Railway/Fly sidecar decision for audio fallback (informed by Phase 2 caption-fetch reliability data)"

    Update .planning/PROJECT.md Key Decisions table: Add rows for:
    - "First real OpenAI key deployed YYYY-MM-DD" | "Phase 2 shipped" | Phase 2 Complete
    - "All 8 cost defenses verified live in production" | "Non-negotiable per PROJECT.md" | Phase 2 Complete

    Update .planning/REQUIREMENTS.md: Mark the following as [x] complete:
    EXTR-01, EXTR-02, EXTR-03, EXTR-04, COST-01, COST-02, COST-03, COST-04, ERRS-04, OPS-04, OPS-05
    Update the traceability table status from "Pending" to "Complete" for each.
    Update Coverage section: Phase 2: 11 requirements (all complete).
    Update ROADMAP.md Phase 2 Plans section: Update to "Plans: 7/7 plans executed" and mark all plan checkboxes as [x].
  </action>
  <verify>
    <automated>grep -c "Phase 2 shipped" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md contains "Phase 2 shipped" with the ship date
    - .planning/STATE.md completed_phases: 2
    - .planning/PROJECT.md Key Decisions table contains "first real OpenAI key deployed"
    - .planning/REQUIREMENTS.md EXTR-01 through OPS-05 all show [x]
    - .planning/ROADMAP.md Phase 2 plans show 7/7 executed
  </acceptance_criteria>
  <done>Planning artifacts updated. Phase 2 complete. Ready to plan Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Vercel dashboard | Owner-action to set EXTRACT_MODE=real; no code can do this |
| Developer → OpenAI dashboard | Owner-action to set budget alert; no code can do this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Elevation of Privilege | EXTRACT_MODE=real before defenses verified | mitigate | Task 1 audit must pass before Task 3 flip; this plan structure enforces the sequencing |
| T-02-07-02 | Denial of Service | YouTube IP blocking on Vercel production | accept | RESEARCH Pitfall 1 documents this risk; Phase 3 adds the audio fallback path; Phase 2 ships captions-only with this known limitation |
| T-02-07-03 | Information Disclosure | OpenAI API key in Vercel Production | mitigate | Key is stored in Vercel's encrypted env var store; never logged or exposed in SSE events |
| T-02-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Task 1 grep assertions all pass (all 7 defenses verified)
pnpm test --run exits 0
pnpm build exits 0
production URL https://exercised-ten.vercel.app returns real extraction for a captioned workout video
STATE.md updated with Phase 2 completion
</verification>

<success_criteria>
All 8 cost defenses verified in codebase. OpenAI dashboard budget alert configured. Vercel Production has EXTRACT_MODE=real. A real captioned YouTube workout video extracts and renders correctly at https://exercised-ten.vercel.app. Planning artifacts updated. Phase 2 complete.
</success_criteria>

<output>
Create `.planning/phases/02-real-captions-pipeline-cost-protections/02-07-SUMMARY.md` when done
</output>
