---
phase: 02-real-captions-pipeline-cost-protections
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - components/extract/ConfidenceBanner.tsx
  - components/extract/ErrorState.tsx
  - components/extract/ExtractFlow.tsx
  - components/workout/WorkoutView.tsx
  - app/about/page.tsx
  - app/api/cron/smoke/route.ts
  - vercel.json
  - tests/confidence-banner.test.ts
  - tests/smoke-cron.test.ts
autonomous: false
requirements:
  - ERRS-04
  - OPS-04
  - OPS-05

must_haves:
  truths:
    - ConfidenceBanner renders amber above WorkoutHeader when lowConfidence=true (D-23)
    - ConfidenceBanner has role="status" and is dismissible (D-23f)
    - ErrorState handles BUDGET_EXHAUSTED code with D-20e copy (D-20e)
    - ExtractFlow passes lowConfidence and cached flags from SSE result event to WorkoutView
    - WorkoutView renders ConfidenceBanner when lowConfidence prop is true
    - /about page contains all three D-24 sections with exact required text fragments
    - /about page is reachable from footer via "Terms & DMCA" link
    - app/api/cron/smoke/route.ts authenticates via Authorization: Bearer CRON_SECRET (D-27)
    - vercel.json contains crons entry with path /api/cron/smoke and schedule "0 9 * * *" (D-27a)
    - Smoke test sends Resend email + opens GitHub Issue on failure (D-27c, D-27d)
    - WorkoutView renders a "cached" badge when cached prop is true (D-26d, POLI-08)
  artifacts:
    - path: components/extract/ConfidenceBanner.tsx
      provides: Amber dismissible banner above WorkoutHeader
      contains: "role=\"status\""
    - path: app/about/page.tsx
      provides: DMCA + ToS + AI-disclaimer static page
      contains: "Exercised extracts workout routines"
    - path: app/api/cron/smoke/route.ts
      provides: Daily smoke-test cron handler
      contains: "CRON_SECRET"
    - path: vercel.json
      provides: Cron job configuration
      contains: '"path": "/api/cron/smoke"'
  key_links:
    - from: components/extract/ExtractFlow.tsx
      to: components/extract/ConfidenceBanner.tsx
      via: lowConfidence prop from SSE result event
      pattern: "lowConfidence"
    - from: app/layout.tsx or components/layout/Footer.tsx
      to: app/about/page.tsx
      via: "Terms & DMCA" link in footer
      pattern: '"/about"'
    - from: vercel.json
      to: app/api/cron/smoke/route.ts
      via: Vercel Cron invocation
      pattern: '"/api/cron/smoke"'

user_setup:
  - service: resend
    why: "Smoke-test failure email alerts (D-27c)"
    env_vars:
      - name: RESEND_API_KEY
        source: "https://resend.com/api-keys → Create API Key"
    dashboard_config:
      - task: "Verify a sender domain or use Resend's sandbox domain for dev"
        location: "https://resend.com/domains"
  - service: github-token
    why: "Smoke-test opens a GitHub Issue on failure (D-27d)"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub → Settings → Developer settings → Personal access tokens → New token (classic) with repo scope"
      - name: GITHUB_REPO
        source: "Set to 'username/exercised' (your GitHub username + repo name)"
  - service: cron-secret
    why: "Vercel Cron authenticates with Authorization: Bearer CRON_SECRET"
    env_vars:
      - name: CRON_SECRET
        source: "Generate: openssl rand -hex 32 — set in Vercel Environment Variables"
  - service: alert-email
    why: "Smoke-test alert recipient (D-24d BLOCKER)"
    env_vars:
      - name: ALERT_EMAIL
        source: "BLOCKER: must be a reachable email. hello@exercised.app is a placeholder. Set to a personal email or configure the domain forwarder before launch."
---

<objective>
Ship the four user-visible Phase 2 UI features and operational infrastructure: the low-confidence banner (ERRS-04), BUDGET_EXHAUSTED error state (D-20e), cached badge (D-26d), /about DMCA page (OPS-04), and the daily smoke-test cron handler (OPS-05). These are all independent of each other and of the real pipeline wiring — they depend only on Wave 3 (route.ts) having added the new SSE fields.

Purpose: These items are cost defenses or operational requirements that MUST ship in the same release as EXTRACT_MODE=real. This plan ensures they exist before the Plan 02-07 ship gate audit.

Output: ConfidenceBanner, BUDGET_EXHAUSTED ErrorState, cached badge, /about page, smoke cron with auth + email + GitHub Issue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-real-captions-pipeline-cost-protections/02-CONTEXT.md
@.planning/phases/02-real-captions-pipeline-cost-protections/02-RESEARCH.md
@components/extract/ExtractFlow.tsx
@components/extract/ErrorState.tsx
@components/workout/WorkoutView.tsx
@components/layout/Footer.tsx
@vercel.json
@app/layout.tsx

<interfaces>
<!-- ConfidenceBanner props (new component) -->
interface ConfidenceBannerProps {
  onDismiss: () => void;
}
// D-23f: amber/yellow accent; role="status"; above WorkoutHeader; dismissible

<!-- WorkoutView props (existing — add lowConfidence and cached) -->
interface WorkoutViewProps {
  workout: Workout;
  shouldAnimateIn: boolean;
  shareLinkOmittedFields?: StripField[];
  lowConfidence?: boolean;   // NEW — from SSE result event
  cached?: boolean;          // NEW — D-26d cached badge
}

<!-- ExtractFlow FSM success state (components/extract/reducer.ts) -->
// success state must store lowConfidence and cached from the SSE result event
// Add to success action payload: lowConfidence?: boolean, cached?: boolean

<!-- SSE result event shape (after Plan 02-04) -->
{ type: "result", workout: Workout, lowConfidence?: boolean, cached?: boolean }

<!-- Smoke cron route -->
export const runtime = "nodejs";
export const maxDuration = 60;
export async function GET(request: NextRequest): Promise<Response>
// Auth: Authorization: Bearer CRON_SECRET
// On success: return 200 OK
// On failure: send Resend email + open GitHub Issue + return 200 (do not fail the cron invocation itself)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfidenceBanner + BUDGET_EXHAUSTED ErrorState + cached badge + ExtractFlow/WorkoutView wiring</name>
  <files>
    components/extract/ConfidenceBanner.tsx,
    components/extract/ErrorState.tsx,
    components/extract/ExtractFlow.tsx,
    components/extract/reducer.ts,
    components/workout/WorkoutView.tsx,
    tests/confidence-banner.test.ts
  </files>
  <read_first>
    components/extract/ErrorState.tsx,
    components/extract/ExtractFlow.tsx,
    components/extract/reducer.ts,
    components/workout/WorkoutView.tsx,
    .planning/phases/02-real-captions-pipeline-cost-protections/02-CONTEXT.md (D-23, D-20e, D-26d)
  </read_first>
  <behavior>
    ConfidenceBanner tests (tests/confidence-banner.test.ts):
    - Renders with role="status" attribute
    - Contains D-23e copy: "Heads up — this extraction may be incomplete. Skim the source video for anything we missed."
    - Has a dismiss button; calling it triggers onDismiss callback
    - Styled with amber/yellow (not red — red is ErrorState territory)

    ErrorState acceptance:
    - BUDGET_EXHAUSTED code renders heading "We're popular today."
    - BUDGET_EXHAUSTED body text includes "Daily extraction budget refills at midnight UTC"
    - BUDGET_EXHAUSTED CTA is "Got it" or similar reset action
  </behavior>
  <action>
    Create components/extract/ConfidenceBanner.tsx: A client component ("use client") that renders a dismissible banner with role="status", amber styling (use amber-400/amber-500 Tailwind color or --color-warning CSS var if defined, otherwise inline style amber). Props: onDismiss: () => void. Include the exact D-23e copy. Include an X or close button that calls onDismiss. The banner should be visually distinct from red ErrorState — use amber/yellow border and background tint, NOT the green accent color.

    Update components/extract/ErrorState.tsx:
    - Add BUDGET_EXHAUSTED to the ErrorCode type union
    - Add BUDGET_EXHAUSTED entry to ERROR_CONFIG with heading: "We're popular today.", body: "We've hit our daily extraction limit. Try again tomorrow — the budget refills at midnight UTC.", cta: "Got it"
    - The onRecover handler for BUDGET_EXHAUSTED should reset to idle (same as RATE_LIMITED)

    Update components/extract/reducer.ts: The success action payload must include optional lowConfidence?: boolean and cached?: boolean. The success FSM state must store these fields so WorkoutView can receive them.

    Update components/extract/ExtractFlow.tsx:
    - When receiving a SSE "result" event, dispatch success with workout + lowConfidence + cached from the event
    - In the success render branch, pass lowConfidence and cached props to WorkoutView
    - Handle BUDGET_EXHAUSTED in the error branch (ExtractEventSchema now includes it — parser will produce it)

    Update components/workout/WorkoutView.tsx:
    - Add lowConfidence?: boolean and cached?: boolean props
    - Add local useState for bannerDismissed (false initially)
    - Render ConfidenceBanner above WorkoutHeader when lowConfidence && !bannerDismissed; dismissing sets bannerDismissed to true
    - Render a small "⚡ Cached" badge near the WorkoutHeader (or inline in the header) when cached is true; use the accent color or a subtle badge style per D-26d
  </action>
  <verify>
    <automated>pnpm test -- tests/confidence-banner.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - components/extract/ConfidenceBanner.tsx exists with role="status" attribute
    - ConfidenceBanner contains exact copy: "Heads up — this extraction may be incomplete."
    - ConfidenceBanner has a dismiss button that calls onDismiss prop
    - components/extract/ErrorState.tsx BUDGET_EXHAUSTED entry exists in ERROR_CONFIG
    - ErrorState BUDGET_EXHAUSTED heading contains "We're popular today"
    - ErrorState BUDGET_EXHAUSTED body contains "midnight UTC"
    - components/extract/reducer.ts success state includes lowConfidence and cached fields
    - components/extract/ExtractFlow.tsx passes lowConfidence and cached to WorkoutView
    - components/workout/WorkoutView.tsx renders ConfidenceBanner when lowConfidence=true
    - components/workout/WorkoutView.tsx renders cached badge when cached=true
    - pnpm test -- tests/confidence-banner.test.ts --run exits 0
    - pnpm typecheck exits 0
  </acceptance_criteria>
  <done>ConfidenceBanner, BUDGET_EXHAUSTED error variant, cached badge, and WorkoutView wiring complete</done>
</task>

<task type="auto">
  <name>Task 2: /about page + footer link + smoke-test cron handler + vercel.json</name>
  <files>
    app/about/page.tsx,
    components/layout/Footer.tsx,
    app/api/cron/smoke/route.ts,
    vercel.json,
    tests/smoke-cron.test.ts
  </files>
  <read_first>
    components/layout/Footer.tsx,
    vercel.json,
    .planning/phases/02-real-captions-pipeline-cost-protections/02-CONTEXT.md (D-24, D-27),
    .planning/phases/02-real-captions-pipeline-cost-protections/02-RESEARCH.md (Pattern 7, Code Examples — GitHub Issue Creation, Resend Alert Email)
  </read_first>
  <action>
    Create app/about/page.tsx as a Next.js App Router RSC (no "use client"). Include metadata export with title: "About Exercised" and description: "Terms, AI accuracy, and DMCA contact for Exercised." Three sections (D-24c):
    Section 1 heading "What this is" — body: "Exercised extracts workout routines from public YouTube videos using AI. We don't host, store, or redistribute the source videos."
    Section 2 heading "AI accuracy" — body: "Extractions are AI-generated and may be incomplete or wrong. Always verify form and reps against the source video."
    Section 3 heading "DMCA / takedowns" — body: "If you're a creator and want a video's extraction blocked or removed, email us at hello@exercised.app with the YouTube URL. We respond within 7 business days."
    Style consistent with the dark glassmorphism theme — use .glass-card class for the container, same font tokens as the rest of the app.

    Update components/layout/Footer.tsx: Add a "Terms & DMCA" anchor link to /about, positioned next to the existing AI-disclaimer line. Use Next.js Link component. Keep the same footer style.

    Create app/api/cron/smoke/route.ts:
    - export const runtime = "nodejs"
    - export const maxDuration = 60
    - export async function GET(request: NextRequest): Promise<Response>
    - Auth check: if request.headers.get("authorization") !== "Bearer " + process.env.CRON_SECRET → return new Response("Unauthorized", { status: 401 })
    - Read smoke config: import tests/eval/smoke.json (will be created in Plan 02-06; for now use a placeholder URL or leave as a TODO comment noting smoke.json is created in Plan 02-06). Create the file if missing with a placeholder { "videoId": "dQw4w9WgXcQ", "expectedExerciseCount": 5 } — Plan 02-06 replaces this.
    - Smoke test logic: POST to process.env.VERCEL_URL + /api/extract (or localhost:3000 in dev) with the smoke video URL, consume SSE stream, assert result event has a valid WorkoutSchema, assert exercise count within ±1 of expected.
    - On failure: sendSmokeAlert(details) using Resend SDK + openGitHubIssue(title, body) using GitHub REST API (both from RESEARCH.md Code Examples). Wrap in try/catch — if alerting fails, log to console; do NOT fail the cron handler itself (return 200 always so Vercel doesn't retry).
    - On success: return new Response("OK", { status: 200 })
    - Import Resend from "resend". Import sendSmokeAlert and openGitHubIssue as inline helper functions or in a lib/smoke/ module.

    Write tests/smoke-cron.test.ts: mock fetch + Resend + GitHub API. Assert: valid CRON_SECRET passes auth; invalid token returns 401; successful extraction returns 200; failed extraction calls sendSmokeAlert and openGitHubIssue.

    Update vercel.json: Add "crons" array entry with path "/api/cron/smoke" and schedule "0 9 * * *" (D-27a). Keep the existing "$schema" field.
  </action>
  <verify>
    <automated>pnpm test -- tests/smoke-cron.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - app/about/page.tsx exists as RSC
    - app/about/page.tsx contains "Exercised extracts workout routines from public YouTube videos using AI"
    - app/about/page.tsx contains "hello@exercised.app"
    - app/about/page.tsx contains three distinct section headings (What this is, AI accuracy, DMCA / takedowns)
    - components/layout/Footer.tsx contains a link with href="/about" and text "Terms & DMCA"
    - app/api/cron/smoke/route.ts contains process.env.CRON_SECRET check
    - app/api/cron/smoke/route.ts imports Resend from "resend"
    - app/api/cron/smoke/route.ts contains github.com/repos/ (GitHub Issues API call)
    - vercel.json contains "crons": [{"path": "/api/cron/smoke", "schedule": "0 9 * * *"}]
    - pnpm test -- tests/smoke-cron.test.ts --run exits 0
    - pnpm build exits 0
    - pnpm typecheck exits 0
  </acceptance_criteria>
  <done>/about page live with all three D-24 sections, footer link added, smoke cron implemented with auth + email + GitHub Issue, vercel.json updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — /about page content + footer link + ALERT_EMAIL blocker</name>
  <what-built>App Router static page at /about with three D-24 sections, footer link to /about, and daily smoke-test cron at /api/cron/smoke with auth + Resend email + GitHub Issue on failure.</what-built>
  <how-to-verify>
    1. Run pnpm dev and visit http://localhost:3000/about — confirm three sections visible: "What this is", "AI accuracy", "DMCA / takedowns"
    2. Confirm footer on http://localhost:3000 shows "Terms & DMCA" link that navigates to /about
    3. BLOCKER CHECK: Confirm that ALERT_EMAIL is either: (a) set to a reachable personal email in .env.local, or (b) you own hello@exercised.app and have configured a forwarder. Per D-24d in CONTEXT.md, Phase 2 CANNOT ship to production until this email is reachable. Note whether this blocker is resolved or still pending.
    4. Confirm Resend API key and GitHub token are available (or note as pending — smoke test is operational but alerting degrades gracefully without them).
  </how-to-verify>
  <resume-signal>Type "approved" after confirming the /about page content, or describe any copy issues. Note the status of the ALERT_EMAIL blocker: "email-ready" if resolved, "email-pending" if still needs setup.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron → /api/cron/smoke | Authenticated via CRON_SECRET in Authorization header |
| /api/cron/smoke → GitHub API | GITHUB_TOKEN is a secret; scoped to repo only |
| /api/cron/smoke → Resend | RESEND_API_KEY is a secret; email-send only scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Spoofing | /api/cron/smoke unauthorized invocation | mitigate | CRON_SECRET check: Authorization: Bearer $CRON_SECRET; returns 401 on mismatch |
| T-02-05-02 | Information Disclosure | GITHUB_TOKEN in cron handler | mitigate | Token accessed via process.env.GITHUB_TOKEN; never serialized to response |
| T-02-05-03 | Denial of Service | /about page content injection | accept | Static page with no user input; no XSS surface |
| T-02-05-04 | Information Disclosure | hello@exercised.app exposed in /about | accept | Intentional — DMCA contact is a public-facing requirement (D-24c) |
| T-02-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
pnpm test --run exits 0
pnpm typecheck exits 0
pnpm build exits 0
grep -c '"path": "/api/cron/smoke"' vercel.json returns 1
grep -c 'role="status"' components/extract/ConfidenceBanner.tsx returns >= 1
grep -c '"about"' components/layout/Footer.tsx returns >= 1
grep -c "BUDGET_EXHAUSTED" components/extract/ErrorState.tsx returns >= 1
app/about/page.tsx exists
app/api/cron/smoke/route.ts exists
</verification>

<success_criteria>
ConfidenceBanner renders above WorkoutHeader when lowConfidence flag is set. BUDGET_EXHAUSTED shows friendly copy. Cached badge renders on cache hits. /about page has all three D-24 sections and is linked from footer. Smoke cron handler authenticates, tests extraction, and alerts on failure. vercel.json schedules cron. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-real-captions-pipeline-cost-protections/02-05-SUMMARY.md` when done
</output>
